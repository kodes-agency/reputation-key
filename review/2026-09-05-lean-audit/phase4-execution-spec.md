# Phase 4 execution spec — keep it small

## Context

Phases 0–3 are closed (`LEAN_TRANSFORMATION_PLAN.md` § "Phase 3 close": 1,935 files, +35,195 / −171,250; tables 215 → 144, contexts 15 → 10). Phase 4 is the roadmap's "keep it small" list plus the leftovers the Phase 3 close named. Every anchor below was measured against `main` = `769e1e28` on 2026-09-07 by three read-only scouts; where the roadmap's number is wrong, the measured one is stated and used. The two dependency majors (WP3.0-B step 3) are already on their PRs: TypeScript 6.0.3 (#474 — the only break was TS5101, `baseUrl` dropped) and BullMQ 6.3.4 (#475 — no `Queue#client`, no `paused` state, no legacy `repeat`).

Regime is Phase 3's: one branch and one PR per WP, base `main`, squash, six required checks; local gate `pnpm typecheck && pnpm lint:ci && pnpm test:unit` on `fnm exec --using=22.23.2`, plus `test:integration` when `src/shared/db/**` or a repository changes, `build && check:bundles` when routes/components change, `test:storybook` when stories change; never edit `.github/workflows/review.yml`; every deletion names its replacement or its evidence of none.

## Order and parallelism

```
WP4.1 lint + toolchain ─┐
WP4.4 code (neverthrow, ports, inbox) ─┼─ parallel (disjoint files; package.json touched by 4.1 and 4.2 — rebase, do not fight)
WP4.2 scripts ──────────┘
        └─► WP4.3 docs / ADR / CONTEXT.md (after 4.2: it rewrites `pnpm ops:` literals the docs and one test carry)
WP4.5 WebKit hydration bug — independent, needs a running app
Close — measurements into LEAN_TRANSFORMATION_PLAN.md
```

---

## WP4.1 — Lint and toolchain

**Corrected anchors.** `eslint.config.js` is **1,675** lines, not 1,300; 23 top-level entries; the `eslint-plugin-boundaries` block (`:146-1240`, 1,095 lines, 65% of the file) holds 51 element descriptors (45 types, 23 of them `shared-*` areas), 12 file categories and 72 dependency policies. Its 32-row shared-area policy table (`:651-897`) is labelled "GENERATED from `src/shared/architecture/shared-dependency-policy.ts` and asserted byte-identical by `shared-context-ownership.test.ts`" — **both files were deleted in Phase 1**; nothing generates or asserts it. Stale entries verified by glob: element `release-config → .railway/**` (`:386-389`, policy `:1220-1222`), element `shared-routing → src/shared/routing/**` (`:338-341`, `sharedAreaElements:44`, policy `:862-867`, allow arm `:669`), files glob `services/**/*.ts` (`:149`; zero `.ts` under `services/`, also in `tsconfig.json:8`, `vitest.config.ts:101`, `eslint.config.js:1625`), ignore `**/.tmp-storybook-build/**` (`:66`, no producer). The roadmap's "three stale carve-outs at :1661,1662,1664" are **not** stale at HEAD — all five `max-lines` ignore targets exist. Opposite failure: `src/shared/ai-provider-control/**` (30 files) has no element and classifies through the `src/shared/**` catch-all. `scripts/check-architecture-boundary-controls.mjs` lints 36 in-memory fixtures (25 must-reject, 11 must-accept) against rule ids `boundaries/dependencies`, `boundaries/no-unknown-files`, `boundaries/no-unknown-dependencies|no-unknown`; only **7** shared areas are load-bearing for it (`shared-queries ✗→ shared-jobs`, `shared-health → shared-jobs`, `shared-db`, `shared-auth` (relay.ts → `#/shared/auth/auth` rejected), `shared-domain`, `shared-governance`); the other 16 appear in zero fixtures. `scripts/ci/operational-tooling-coverage.test.ts:38-47` pins two byte-level facts: `files: ['scripts/**/*.{ts,mjs}']` on one line, and the boundaries block's `files` array listing `scripts/**/*.{ts,mjs}` **above** `'boundaries/elements'`.

**Steps.**

1. Collapse the boundaries block to ~150 lines: keep both `no-unknown-*` rules at `error`, `default: 'disallow'`, the `import/resolver` typescript setting, the `files` array (drop `services/**/*.ts`) above `'boundaries/elements'`; ~28 descriptors (`domain`, `application` ×3, `infrastructure`, `server`, `context-ui`, `routes`, `components`, `ui-support` ×2, `shared-domain`, `shared-db`, `shared-auth`, `shared-jobs`, `shared-queries`, `shared-health`, `shared-governance`, `shared` catch-all LAST, `test-helpers` ×3, `top-level`, `runtime-plugin`, `story-fixtures`, `e2e-harness`, `script-ci` ×2, `script-operator`, `script-tooling`); all 12 file categories; ~30 policies — the 11 context/route/UI rows (`:498-644`), the 7 shared rows below, `shared-events → domain`, the test-helper rows, the entry-point rows, 3 script rows + 2 script file-category rows, the 5 outbox-runtime rows (`:1141-1178`, with the allow-list rewritten as `elementTypes('shared','shared-db','shared-domain','shared-jobs')` — **omitting `shared-auth` is what keeps relay.ts → auth rejected**), the 3 deployable rows, stylesheet, story-file.
   ```js
   { from: elementType('shared-domain'),  allow: { to: elementType('shared-domain') } },
   { from: elementType('shared-db'),      allow: { to: elementTypes('shared','shared-auth','shared-db','shared-domain') } },
   { from: elementType('shared-auth'),    allow: { to: elementTypes('shared','shared-auth','shared-db','shared-domain') } },
   { from: elementType('shared-jobs'),    allow: { to: elementTypes('shared','shared-auth','shared-db','shared-domain','shared-jobs') } },
   { from: elementType('shared-health'),  allow: { to: elementTypes('shared','shared-db','shared-domain','shared-health','shared-jobs') } },
   { from: elementType('shared-queries'), allow: { to: elementTypes('shared','shared-domain','shared-queries') } },  // browser-reachable: never the job runtime
   { from: elementType('shared'),         allow: { to: elementTypes('shared','shared-auth','shared-db','shared-domain','shared-governance','shared-health','shared-jobs','shared-queries') } },
   ```
   Delete: the 32-row table, the 16 unexercised `shared-*` elements, `shared-routing`, `release-config`, `shared-outbox-infra` (`:236-244`; its only job is the drizzle carve-out, which is a `no-restricted-imports` file glob), the `sharedAreaElements` const and its 11 spreads, the 30-line taxonomy comment (`:115-145`, restate in ≤6 lines), the five comments citing the deleted generator/test (`:23,135,257,356,647-651`), `'**/.tmp-storybook-build/**'`. Keep `eslint-rules/cross-context-public-api.mjs` verbatim and its block (`:1277-1283`, off for tests `:1631`).
2. **`check:zod-conformance` → a local ESLint rule, not a `no-restricted-imports` entry** (correction): ESLint 10.10.0's `no-restricted-imports` has no `ImportExpression` listener, so dynamic `import('zod')` escapes, and a `no-restricted-syntax` selector for `z.string().<format>()` is binding-blind and, being non-additive across configs, would silently drop the domain-clock (`:1547-1565`) and `process.env` (`:1572-1585`) selectors for overlapping files. Write `eslint-rules/zod-v4.mjs` (~50 lines, registered in the existing `local` plugin, enabled repo-wide): (a) every `ImportDeclaration`/`ExportNamedDeclaration`/`ExportAllDeclaration`/`ImportExpression`/`TSImportEqualsDeclaration` source of `zod` or `zod/*` other than `zod/v4` → "Import the pinned API explicitly from 'zod/v4'."; (b) `<binding>.string().<format>()` for the 20 names in `scripts/review/zod-v4-conformance.ts:28-43`, `<binding>` tracked from `zod/v4` imports (`import { z }`, `import { z as x }`, default, namespace) → "Use z.<format>() — z.iso.datetime() for datetime". Port the 5 cases of `scripts/review/zod-v4-conformance.test.ts` to an `ESLint`-driven test beside `src/shared/architecture/cross-context-public-api.test.ts`; delete the script, its test, `package.json:71`, and the `pnpm check:zod-conformance` link in `lint`. The scan surface (161 files carry a zod specifier) must stay covered: the rule's `files` glob includes `e2e/**`, `.storybook/**`, `scripts/**`.
3. **Node pin — the roadmap's "only in the Dockerfile" is inverted by the tree** (correction): `.nvmrc` is the source seven CI jobs read (`ci.yml:41,89,151,260,824,890`, `simulation.yml:45` via `node-version-file`), and the four Dockerfile `FROM`/assert pairs are inside the AI deployment contract's digest preimage (R2). Keep `.nvmrc` as the single exact pin and the Dockerfiles untouched; relax `package.json` `engines.node` to `">=22.23.2 <23"` (it enforces nothing — `engine-strict` is deliberately absent per `.npmrc:1-14`); make `scripts/check-test-quality.mjs:87` `PINNED_RUNTIME.node` read `.nvmrc` (ICU/Unicode stay literal — Node bundles its own ICU); fix `.npmrc:18` and `check-test-quality.mjs:260,271`, which cite `engines`. The AI language generators/manifests/tests are attested bytes, not configuration — untouched.
4. **`.prettierignore` — the roadmap's "drop the byte-attested carve-outs" would break four unit tests** (correction): the carve-out (`:20-31`) is four hand-written wrappers whose exact bytes `src/shared/ai-language-generated-artifacts.test.ts:59-200` hashes into four attestation digests; reformatting fails 4 of its 5 cases and can only be regenerated on node 22.23.2 / ICU 78.2. Keep the four lines, shrink the 12-line comment to 3, **add** `src/shared/generated/**` and `scripts/generate-ai-*.ts` (also hashed, prettier-clean only by luck), delete the four stale entries (`:4-6` storybook-static / `.tmp-storybook-build` / `.tmp-unit-results.json`, `:17` `docs/release-evidence/baseline/`).
5. Drop `services/**/*.ts` from `tsconfig.json:8`, `vitest.config.ts:101`, `eslint.config.js:149,1625`.

**Tests.** `scripts/ci/operational-tooling-coverage.test.ts` (keep; its two byte pins constrain step 1), `src/shared/architecture/cross-context-public-api.test.ts` + `organization-export-contributor.port.test.ts:30` (keep), the new zod rule test, `scripts/review/zod-v4-conformance.test.ts` (delete with its subject).

**Verification.** `wc -l eslint.config.js` recorded (target ≤ ~750 total, boundaries block ≤ ~160); `node scripts/check-architecture-boundary-controls.mjs` → `25 invalid imports rejected; 11 valid seams accepted`; `pnpm lint:ci` green; `pnpm eslint src/shared/ai-provider-control` classifies 30 files without `no-unknown-files`; a throwaway file with `import { z } from 'zod'` and one with `z.string().uuid()` each fail `pnpm eslint` on the new rule; `pnpm test:unit -- scripts/ci src/shared/architecture src/shared/ai-language-generated-artifacts` green; `cat .nvmrc` = `22.23.2` and `git grep -n "22.23.2" -- package.json` → none.

## WP4.2 — Scripts

**Corrected anchors.** `package.json` has **98** scripts (DEPS.md's 175 was pre-Phase-2). DEPS.md § "Proposed script list" names 26, six of which do not exist in the tree (`build:worker`, `db:check-drift`, `local:up`, `local:down`, `seed` as data, `test` as unit) — it is intent, not a list to paste. Measured: **25** CI-referenced (`ci.yml`), `prepare` is an npm lifecycle script (keep), `.husky/pre-commit` invokes no package script, **12** are referenced by nothing (`dev:all` — sole `concurrently` consumer, `preview`, `seed:simulate`, `db:pull` — `docs/auth-migrations.md:19` forbids it, `ops:deploy-ci-images` — the script file stays per the Railway decision, `ops:reconcile-recent-activity-vocabulary`, `ops:identity-invitation-facts` — subject deleted in WP3.3-C, `build-storybook` — job deleted in WP3.6, `test:unit`/`test` duplicates, `check:test-quality` — `lint:ci` calls the `.mjs` directly, `db:bootstrap-auth` + `audit:member-roles` — sole `dotenv-cli` consumers), and **56 `ops:*` + 5 `perf:*`** entries of which 41 are referenced only by their own USAGE string.

**Steps.**

1. Delete the 12 zero-reference scripts; drop `concurrently` and `dotenv-cli` from devDependencies with them (`pnpm remove`). Keep `test:unit` (the name every regime line uses), delete `test`.
2. **One dispatcher replaces the 56 `ops:*` entries** (DEPS item 16, executed as a command not a README table so operator commands stay one word away): `scripts/ops/index.ts` (~40 lines) maps `<name>` → `scripts/ops/<file>.ts` and execs it with `tsx`, prints the table on no argument; `package.json` gets `"ops": "tsx scripts/ops/index.ts"`. Rewrite every `pnpm ops:<name>` literal to `pnpm ops <name>` across `docs/**` (not `docs/archive`), `scripts/**` USAGE strings, `src/shared/jobs/operational-catalogue.ts:24` (`GENERIC_REPAIR_COMMAND`), `src/shared/observability/beta-support-operations.ts:23,34`, and the three test literals in `beta-support-operations.test.ts:113-116` (they read `runbooks.md §16` — keep the doc and the test agreeing). `perf:*` (5) stay.
3. Keep `clean`, `lint:fix`, `test:watch` (DEPS list; one line each). `check:zod-conformance` disappears with WP4.1; `check:typescript-project-coverage` stays (still called by `typecheck`).

**Verification.** `node -e 'console.log(Object.keys(require("./package.json").scripts).length)'` recorded (expected ≈ 42 from 98); `git grep -n "pnpm ops:" -- . ':!docs/archive' ':!review'` → none; `pnpm ops` prints the table and `pnpm ops queue --help` (or the equivalent read-only command) runs; `pnpm lint:ci`, `pnpm test:unit -- src/shared/observability src/shared/jobs scripts/ci` green; CI's 25 referenced names unchanged (`grep -oE "pnpm [a-z:-]+" .github/workflows/ci.yml | sort -u` diff empty).

## WP4.3 — Docs, ADRs, CONTEXT.md

**Corrected anchors.** 100 active `.md` under `docs/` (12,875 lines; 16 load-bearing — referenced from code, CI, README or BETA.md — 25 operational, 42 ADR incl. README, 10 release-evidence snapshots, 7 other). **`docs/adr/README.md` has no §6** — it is a 59-line index; the buckets live in `review/2026-09-05-lean-audit/ADR.md` (grouped RETIRE 12 / MERGE 13 / AMEND 11 / KEEP 13) and reconcile exactly with the 41 files present: 6 RETIRE still present (0001, 0002, 0005, 0009, 0020, 0048), 11 MERGE still present (0003→0055, 0004→0055, 0006→0052, 0010→0056, 0011→0046, 0012→0015, 0018→0019, 0022→0046, 0039→0052, 0045→0056, 0047→0032), 11 AMEND, 13 KEEP. Four bucket changes since the audit: 0042 is superseded by WP3.3-A → RETIRE; 0057 is superseded by `docs/BETA.md` but still carries the "Retained from ADR 0054" section → move those retained sentences into `BETA.md` §1 and RETIRE; 0006 and 0047 declare themselves superseded by WP3.4/WP3.3-C without their merge targets having absorbed anything → check, then RETIRE. 14 of 41 ADRs have YAML frontmatter (fields `status`, `date`, `superseded_by`; 0050 adds `release_state`/`accepted_date`/`amended_date`), 27 use `**Status:**` prose. `runbooks.md` is 987 lines / 24 sections and is **test-pinned by path**: `incident-runbook-catalogue.test.ts:30,49` (§1,2,6,7,8,12,19,20,21 as `^## N. ` headings, 8 labelled blocks each, 5 literal strings incl. the two "Default incident commander/communications" lines), `alert-definitions.test.ts:171-175` (19 `runbook: 'runbooks.md §N'` fields resolve), `beta-support-operations.test.ts:113-116` (three literals inside §16). 15 `src/**/CONTEXT.md` = 2,893 lines; 7 context files still carry the four tables (`portal` 342, `inbox` 323, `ai` 284, `guest` 240, `review` 214, `integration` 189, `property` 165); `feed` 69 / `reporting` 69 / `identity` 74 are the WP3.4 template. **Nothing parses CONTEXT.md any more** (`context-public-interface-authority.ts` and `documentation-authority.test.ts` are gone). 12 source comments cite CONTEXT.md by line number and 10 code sites cite doc paths that no longer resolve.

**Steps.**

1. **ADRs.** Delete the 6 + 4 RETIRE (0001, 0002, 0005, 0009, 0020, 0048, 0042, 0057 after moving its two retained 0054 sentences into `docs/BETA.md` §1 within the 120-line cap, 0006, 0047 after confirming 0052/0032 already state what survives). Execute the 9 remaining merges: carry the target's still-true decisions into the named target under a "Merged from ADR NNNN" heading (verbatim, no rewrite), delete the source. Amend the 11 AMEND files per their audit row. Normalise frontmatter on every retained file to exactly `status: accepted|proposed|superseded`, `date`, optional `supersedes`, optional `superseded_by` (ADR number or `docs/BETA.md`); fold 0050's three extra keys into `date` + a body line. Write `scripts/adr-index.mjs` (~40 lines) that renders `docs/adr/README.md` from the frontmatter (number, title from the H1, status, superseded_by) with the missing-number note derived from the gaps — run it, commit the output, no drift check (a generated index that is one command away is cheaper than a gate). Update `docs/adr/README.md:10-11`'s missing-number list by derivation.
2. **CONTEXT.md.** Rewrite the 7 context files to the template (`## Responsibility|Scope`, `## Boundaries`, `## Model`, `## Runtime`, `## Invariants`, `## Verification`), ≤ 80 lines, deleting `## Events produced`, `## Events consumed`, `## Architecture layers`, `## Public API`; keep every denial/residual-risk sentence verbatim. Cap `src/contexts/CONTEXT.md` (267), `src/routes/CONTEXT.md` (196), `src/shared/CONTEXT.md` (188), `src/shared/db/CONTEXT.md` (143), `src/components/CONTEXT.md` (130) at ~80 by deleting the layer tables and the rules that a lint or test now enforces (name the enforcer in a one-line "enforced by" note). Replace the 12 line-number citations (`components/inbox/reply-form.tsx:1`, `inbox-notes-thread.tsx:7`, `types.ts:5`, `use-inbox-state.ts:2`, `contexts/CONTEXT.md:98,250`, …) with section names.
3. **Docs to 55.** Archive under `docs/archive/2026-09-lean/` (git mv, no rewrite): the 10 `release-evidence/review/*` + `release-evidence/beta/README.md`; operations docs whose command or migration no longer exists — `legacy-people-team-contraction`, `legacy-recognition-contraction`, `legacy-import-control-contraction`, `legacy-rollup-contraction`, `recent-activity-identifier-cutover`, `review-source-content-cutover`, `current-google-reputation-snapshot`, `ai-personalized-reply-draft`, `oauth-state-record-key-cutover` (after lifting its `:9-20` rationale into ADR 0050 §5); the three rehearsal checklists (`bqc0-stop-control-rehearsal` — its live procedures are restated in runbooks §11 —, `bqr6-recovery-rehearsal`, `bqr6-staging-load-fault-checklist`); `design/a11y-responsive-audit.md` + `design/contrast-audit.md` (cite the deleted `pnpm test-storybook`); `dev-notes/depcheck-false-positives.md`. Fold the five live-command invocation contracts (`inbox-handling-cycle-cutover`, `compatibility-read-contraction`, `non-fk-reference-inventory`, `guest-response-reconciliation`, `portal-beta-readiness-reconciliation`, 679 lines) into one `docs/operations/operator-reports.md` (~120 lines: command, what it reports, when to run). Fold `legal/CHANGELOG.md` (3 lines) into `BETA.md` §10. Split `runbooks.md` the only test-safe way: keep the preamble + §1–21 in place; lift `## Operator commands` (`:21-95`) → `docs/operations/operator-commands.md` and `## Security posture` (`:96-209`) + `## Alerts` (`:921-987`) → `docs/operations/alerts-and-security-posture.md`; fix the three `ops:` commands §10 cites that do not exist. Fix the 10 dangling citations (`src/shared/CONTEXT.md:87`, `scripts/check-filenames.mjs:61`, `src/shared/jobs/runtime-authority.test.ts:33`, `src/shared/ai-openai-request-contract.ts:8`, `src/shared/testing/configured-database-fence.ts:9`, `src/shared/testing/staging-cell.ts:30` + `.test.ts:7`, `scripts/migrations/0000-auth-tables-bootstrap.sql:8`, `src/shared/governance/entry-point-catalogue.ts:29`, `src/bqc/status-schema.test.ts:16`) to the archived path or delete the citation. **Do not move or reflow** `docs/archive/2026-09-lean/…/ai-private-beta-policy-v1.md` (its sha256 is recorded in `ai-private-beta-policy-v1.manifest.json:15-17`).

**Verification.** `git ls-files 'docs/*.md' 'docs/**/*.md' | grep -v '^docs/archive/' | wc -l` recorded (expected 55); `wc -l docs/adr/*.md | tail -1` and `ls docs/adr | wc -l` recorded (expected 24 files incl. README); every `src/**/CONTEXT.md` ≤ 80 lines (`for f in $(git ls-files 'src/**/CONTEXT.md'); do wc -l $f; done`); `pnpm test:unit -- src/shared/observability src/shared/governance scripts/ci src/shared/jobs` green (the runbook-pinned tests); `git grep -nE "docs/[-A-Za-z0-9_./]+\.md" -- src scripts .github README.md | while read …` every cited path exists; `node scripts/adr-index.mjs && git diff --exit-code docs/adr/README.md`.

## WP4.4 — Code: neverthrow, ports, the inbox header

**Corrected anchors.** Ports: 169 files / **11,129** lines (not 12,927), **79** `*Port` names (not 85), and exactly **one** `export interface` — they are `Readonly<{…}>` type aliases. Of 194 port-like types, 133 have one production implementation, 53 none, **7** genuinely two or more (`OrganizationExportContributor` ×14, `OrganizationLifecycleContributor` ×11, `PropertyEraseContributor` ×3, `EmailSenderPort`, `InboxItemLookupPort`, `GoogleAuthorizedProviderExecutor`, `GoogleImportReferenceStore`). **"Inline single-implementation ports" cannot mean the 133**: their consumers are application use cases, and `application` may not import `infrastructure` (boundaries), so the type has nowhere to go but `application/ports`. The weightless ones are the ports whose only implementation is an inline object literal in `build.ts` — **13** (`ai/property-reply-language` 12 lines, `feed/portal-health-lookup` 21, `guest/guest-snippet-read` 44, `guest/primary-staff-attribution` 13, `integration/property-lookup` 21, `property/property-lifecycle-readiness` 10, `reporting/goal-execution-policy` 20, `reporting/portal-lifetime-metrics` 43, `reporting/portal-metrics` 82, `reporting/portal-response-integrity` 14, `identity/auth-session` 33, `identity/member-authority-lifecycle` 37, `identity/member-offboarding` 62) — plus one fully dead port, `identity/application/ports/portal-responsibility-lookup.port.ts` (11 lines, zero importers). `neverthrow`: **one** code importer (`src/shared/domain/result.ts:4`, re-exporting `Result`, `ok`, `err`) but load-bearing through the `#/shared/domain` barrel for 17 domain files and 66+ `.isOk()/.isErr()` consumers; API actually used: `Result` type, `ok`, `err`, `.isOk`, `.isErr`, `.value`, `.error`, `.map` (8 sites), `Result.combine` (5 sites, one with a heterogeneous 5-tuple at `portal/domain/constructors.ts:65`), `.match` (1 site), `_unsafeUnwrap`/`_unsafeUnwrapErr` (6 test files); zero `ResultAsync`/`andThen`/`mapErr`. Inbox header: `inbox.mapper.ts:34-41` + `inbox-command-store.ts:167-170` null all seven enrichment fields (`rating`, `snippet`, `reviewerName`, `propertyName`, `contentAvailability`, `reviewLanguageCode`, `attention`) on every command result, and `inbox-cache-policy.ts:76-80 patchItem` replaces the whole cached item, reached from all three status mutations; `feedback-handling.store.ts:79-88` already restores the seven from a snapshot on the feedback path.

**Steps.**

1. `src/shared/domain/result.ts` becomes the ~45-line local module: `Ok`/`Err` objects with `isOk`/`isErr` type guards, `value`/`error`, `map`, `match`, `_unsafeUnwrap`/`_unsafeUnwrapErr`, and `Result.combine` with a tuple-preserving overload (`<T extends readonly Result<unknown, E>[]>` → `Result<{ [K in keyof T]: T[K] extends Result<infer V, E> ? V : never }, E>`) so the five call sites keep their destructuring types. No importer changes (all go through `#/shared/domain`). `pnpm remove neverthrow`; delete `src/shared/CONTEXT.md:103`'s mention.
2. Delete the dead port and the 13 build.ts-literal ports: type each literal where it is built (a local `type` in `build.ts` or the consuming use case's own parameter type), delete the port file, drop the barrel export if any. `pnpm typecheck` finds every consumer.
3. `inbox-cache-policy.ts patchItem`: merge the command item over the cached item but keep the cached value for the seven enrichment-only keys (blacklist, not `??`, since both call sites pass a command snapshot). Route `onFeedbackHandlingChanged` through the same merge. Apply the same treatment to the list cache path (`use-inbox-page.ts:126-127`). Add the case `inbox-cache-policy.test.ts` lacks (cached `propertyName: 'Acme Hotel'`, `reviewerName: 'Dana'` survive a `{ id, status: 'closed', commandRevision: 3 }` patch while `status`/`commandRevision` advance); the existing `:72` case still passes and proves nothing (`toEqual` ignores `undefined`) — rewrite it to the merged shape.

**Verification.** `pnpm typecheck`, `pnpm lint:ci`, `pnpm test:unit` green; `git grep -n neverthrow -- src scripts package.json` → none; `ls src/contexts/*/application/ports/*.ts | wc -l` = 155; live: on the dev server, escalate an inbox item in a second tab — the header keeps the property and reviewer names before any refetch (the Phase 3 close recorded the placeholder).

## WP4.5 — WebKit hydration (React #418)

Revision item 6 recorded a 2/21 failure on `compat-webkit` and `compat-mobile-ios`: React error #418, a hydration text mismatch on the authenticated manager shell, identical with the sharding change reverted, invisible to Linux CI. Reproduce on macOS Playwright WebKit against the host app (dev server + worker + stubs, seeded), capture the first `#418` with the server-rendered vs client text (`page.on('console')` and the hydration diff React logs in development), find the render that depends on `navigator`/`Intl`/`Date` at hydration time, fix it in the component (or gate the client-only value behind `useSyncExternalStore`/an effect), prove it with the same run going 0 errors, and run `pnpm test:e2e:compatibility --project=compat-webkit` against the host app. If it cannot be reproduced on the host build, file the issue with the exact evidence and the repro command instead.

## Close

Record in `LEAN_TRANSFORMATION_PLAN.md` § "Phase 4 progress": per WP the measured delta and every correction above that held or did not; a Phase 4 close table with `eslint.config.js` lines, active docs, ADR files/lines, CONTEXT.md lines, `package.json` scripts, port files/lines, tracked files, TS lines, unit wall, CI wall; the two majors; the WebKit outcome.
