# Phase 5 execution spec — the leftovers, the legal gates, and the last machinery

## Context

Phases 0–4 are closed (`LEAN_TRANSFORMATION_PLAN.md` § "Phase 4 close": `main` = `6738e364`). `review/2026-09-05-lean-audit/` rows that never entered a WP, the plan's own recorded deferrals, and the lawful/regional decisions the owner confirmed on 2026-09-08 are executed here. Every anchor below was measured on 2026-09-08 against `main` = `6738e364` by four read-only scouts; their full reports sit beside this file (`scoutlegal.md`, `scoutbundle.md`, `scoutceremony.md`, `scoutseam.md`) and are binding where this spec summarises them. Where the audit's or the plan's number was wrong, the measured one is stated and used.

Owner decisions applied throughout (2026-09-08): the privacy notice and the Google access disclosure are **accepted for publication** by the accountable owner (Bozhidar Denev) without external counsel review — the record says so; the regional-processing claim to Google is **restated** (global OpenAI API, US-only hosting); the corrected 24-hour caching sentence is **re-notified** to Google in the same letter; the rights channel is `denev@kodes.agency`.

## Regime (Phase 3/4's, unchanged)

One branch and one PR per WP, base `main`, squash, six required checks; local gate `pnpm typecheck && pnpm lint:ci && pnpm test:unit` on `fnm exec --using=22.23.2`, plus `test:integration` when `src/shared/db/**` or a repository changes, `build && check:bundles` when routes/components/vite config change, `test:storybook` when stories or the storybook config change; never edit `.github/workflows/review.yml`; every deletion names its replacement or its evidence of none; **commit after every completed step**; never add a row to `src/shared/governance/entry-point-catalogue.ts` (deleting rows is in scope); a job family with a schedule that is deleted adds its name to `RETIRED_SCHEDULER_JOB_NAMES` in the same commit.

## Order and parallelism

```
wave 1 (parallel, disjoint files):  WP5.0 legal ─ WP5.1 bundle ─ WP5.2a ceremony ─ WP5.2b hero-image ─ WP5.3a container seam
wave 2 (after 5.2a and 5.2b merge): WP5.3b catalogue slimming
then: full plan review → fixes → browser verification of every capability → close
```

Known overlap, resolved on rebase, not by ownership: `src/bootstrap.ts` and `src/composition.ts` (5.2b deletes hero registrations; 5.3a retypes and moves the claim — different regions); `entry-point-catalogue.ts` / `event-job-catalogue.ts` (5.2a deletes `ops(` rows :3588-3758 and the stale row :4084-4092; 5.2b deletes the job/consumer rows for the two hero jobs — different regions); `src/routeTree.gen.ts` (5.0 adds `/privacy`; regenerate after any rebase with `pnpm build`).

---

## WP5.0 — The lawful and regional gates

**Anchors (scoutlegal.md §1–6).** `docs/legal/privacy-notice.md` 271 lines / 9 sections, `status: draft`, 11 owed markers (:2, :7, :9, :12-15, :31-32, :62-64, :113-117, :144-146, :165-166, :190-191, :211-216, :232-238, :253-256, :260-263, :270-271). `docs/legal/google-access-disclosure.md` is the same shape (draft, linked from the notice at :66). Merchant AI notice links: `merchant-ai-notice-contract.ts:89` `/privacy`, `:121` `/privacy#contact` — **unchanged** (changing them re-collects consent). No markdown renderer in the tree; `browser-reachability.test.ts` forbids `node:*` in any route's import graph. CSP (`security-headers.ts:126-152`): `style-src 'self' 'unsafe-inline'`, `script-src 'self' 'nonce-…'`. `__root.tsx:54-60` gives every non-portal route the Header/Footer chrome; the guest portal (`/p/$token`) has no chrome and no privacy link. No catalogue row is needed for a `route_ui` (nothing reads `ROUTE_UI_ROWS`). The rights channel does not exist in code; `denev@kodes.agency` is the address on the Google letter (`…clarification.md:5`).

**Steps.**

1. **Complete the notice** (`docs/legal/privacy-notice.md`) from code facts, every owed marker replaced by a statement — keep the existing sections and the retention table, which already matches the sweep (scoutlegal.md §2 table; the three rows it flags stay as written: session pseudonym 24 h, feedback 90 d by stamped deadline, de-identified fact 24 months). Fill: §1 roles and legal bases — Kodes Agency ("Reputation Key") is the **controller** for Participant account data, security/monitoring signals and beta feedback (legal basis: performance of the beta agreement, `docs/legal/internal-beta-agreement.md`, and legitimate interests for security, abuse prevention and diagnostics); for Google Business Profile content and Guest Portal submissions the **Organization is the controller** and Kodes Agency the **processor** on its documented instructions; AI features run only on the Organization administrator's recorded opt-in (`MERCHANT_AI_NOTICE_VERSION` re-consent chain); Guest private ratings and feedback are voluntary submissions processed in the Organization's legitimate interest in service quality. §2 operational: the subprocessor schedule as a table — Railway (hosting, PostgreSQL, Redis, object storage; United States, US West) · OpenAI (AI inference; global API endpoint, no residency commitment, `providerResidencyClaim: 'none'`, prompt cache 24 h, abuse-monitoring retention generally ≤ 30 days, no training without organization opt-in — `ai-openai-provider-profile.ts:72-86,202`) · Google (Business Profile APIs and OAuth; independent controller of Business Profile data) · Resend (transactional email; United States) · Sentry (error monitoring, United States; only when a DSN is configured — none is today). §4 transfers: all providers process in the United States; EU/UK transfers rely on each provider's data-processing terms incorporating the EU Standard Contractual Clauses and, where the provider is certified, the EU-U.S. Data Privacy Framework; **no EU deployment exists in the beta**. §6 the "still needs accepted rules" list becomes rules: base Guest visit/destination facts — content-free, 24 months; legacy Guest rows — none (the beta database started empty on 2026-09-05); account/member data — for the life of the membership, purged by the Organization closure workflow (`docs/operations/organization-lifecycle.md`); manager-authored replies, notes, Portal configuration and publication history — for the life of the Organization, purged at closure; Contact Requests — disabled; provider reply content — the 30-day source policy; AI derivatives — 24 months de-identified, withheld immediately when the authorization is withdrawn; application logs — content-minimized, held by the hosting provider for at most 30 days; quarantine — none (no quarantine tables remain); object storage — export objects 7 days, retrieval links 24 h; backups — the hosting provider's encrypted backups for at most 30 days, and every erasure is recorded in the append-only `backup_erasure_ledger` and re-applied after any restore. §7 rights: channel `denev@kodes.agency`; identity verification — Participants write from their registered address, Guests present the signed response link or the submission details; response within 30 days; escalation to the same address marked "escalation", then the competent supervisory authority; backups per §6. §8: replace the `cell-us` paragraph with "one production deployment on Railway in the United States (US West); no EU deployment"; delete the "before publication, retain…" paragraph (it is an operator checklist, not a notice). §9: effective date **2026-09-08**, version **2.0**, material changes announced to Participants by email at least 14 days before they take effect with the version and date shown on the page; data-protection and security contact `denev@kodes.agency`; delete the draft trailer. Front-matter `status: accepted`, `accepted_by: Bozhidar Denev`, `accepted_on: 2026-09-08`. Do the same to `google-access-disclosure.md` (status, date, delete its do-not-publish block; keep its content).
2. **Publish.** Add `marked` (runtime dependency, exact version) and a route `src/routes/privacy.tsx` — `createFileRoute('/privacy')`, static: `import notice from '../../docs/legal/privacy-notice.md?raw'` (Vite raw import; no `node:*`), rendered on server and client with `marked` (GFM, headings get `id` slugs; the `## 9. Changes and contact` heading gets `id="contact"` so `/privacy#contact` resolves — simplest is an explicit `<h2 id="contact">` line in the markdown, which `marked` passes through), `head()` sets the title, a `<article class="prose …">` wrapper with the app's typography classes, no `dangerouslySetInnerHTML` of anything but this in-repo string. Second route `src/routes/privacy_.google-access-disclosure.tsx` at `/privacy/google-access-disclosure` for the disclosure; rewrite the notice's link at :66 to that path. Regenerate and commit `src/routeTree.gen.ts`. Add a "Privacy notice" link to `src/components/layout/footer.tsx` and to the guest portal footer in `src/components/features/guest/public-portal/public-portal-content.tsx` (Guests are covered by the notice and the portal has no chrome). Verify `browser-reachability.test.ts` and `guest-contact-containment.test.ts` still pass; `pnpm build && pnpm check:bundles` (the route is a lazy chunk; `marked` must not enter the entry closure).
3. **Restate to Google.** New `docs/external/google/google-business-profile-ai-processing-restatement-2026-09-08.md` in the outbound letter's format (`…clarification.md:1-9`): purpose — restate two facts before further activation: (a) processing region — the beta uses OpenAI's global API endpoint with no regional processing profile and no provider residency commitment; hosting is a single United States deployment; the earlier statement "route through the property's approved US/EU/other processing profile" (`…response-2026-07-14.md:104`) is withdrawn for the beta; (b) prompt-cache retention — the merchant notice now states 24-hour extended prompt caching (`MERCHANT_AI_NOTICE_VERSION = 'merchant-ai-notice-2026-09-06.v1'`), correcting the earlier "at most one hour"; `Response status: pending — not yet sent`. Add it to the response document's Evidence set (`…response-2026-07-14.md:11-14`). **Sending is the owner's action**; record that in `docs/BETA.md`.
4. **`docs/BETA.md` §2**, staying ≤ 120 lines: :24 → `RESTATED 2026-09-08` (the letter's path; sending pending); :25 → `RESOLVED 2026-09-08` (published at `/privacy`, `/privacy#contact`, `/privacy/google-access-disclosure`; accepted by the owner without external counsel); :22 and :29 → `ACCEPTED 2026-09-08` (retention matrix and the nine duties, with the notice as the record); :30 stays (proof of live erasure is still not a repository fact); fix the six stale anchors scoutlegal.md §6 lists (`:61,176`, `:89,121`, `:13,180`, `source-content-policy.ts:39-40,49-56`, `retention-sweep.job.ts:55-106`, the notice line numbers). `LEAN_TRANSFORMATION_PLAN.md` is edited by the close, not here.

**Tests.** None deleted. New: none required — `browser-reachability` and `guest-contact-containment` walk the new routes; add one case to `src/routes/-unavailable.test.ts`'s neighbour pattern only if a route test exists for static pages (it does not; skip).

**Verification.** `pnpm typecheck && pnpm lint:ci && pnpm test:unit -- src/shared/architecture src/routes` green; `pnpm build && pnpm check:bundles` green with `marked` outside the closure; `wc -l docs/BETA.md` ≤ 120; live: `GET /privacy` 200 rendering the notice with the version and date, `/privacy#contact` lands on the contact section, `/privacy/google-access-disclosure` 200, the AI settings notice's two links resolve, the guest portal footer links to `/privacy`; `git grep -n "Do not publish\|Candidate draft\|2.0-draft\|status: draft" docs/legal` → none.

## WP5.1 — The client bundle

**Anchors (scoutbundle.md).** Entry `index-*.js` **99,117 B** gzip (37,896 on 2026-09-05), closure **580,221 B** (112 js + 1 css); target 200 KiB unreachable — recomputed floor **≈ 330,000 B** with every addressable edge cut, ≈ 303,000 with Sentry deferred. Two mechanisms, not one: (1) value-imports from component barrels in route config/loaders; (2) `vite.config.ts:86-94` `app-shared` group test `/src/(components|contexts)/` welds `src/contexts/*/server/*.ts` stubs into the same chunk as feature components, so a loader importing only a server fn pins the components. (3) The Sentry SDK (86,601 raw / ≈ 26,700 gz) is inlined into the entry because `src/start.ts:7-10` imports `@sentry/tanstackstart-react` statically while `instrument.client.ts:12-16` imports it dynamically.

**Steps (measured bytes freed in scoutbundle.md §3).**

1. `vite.config.ts` groups: split `app-shared` into `app-server-fns` (`/src/contexts/[^/]+/server/`) and `app-components` (`/src/components/`); add `vendor-sentry` (`/node_modules[\\/]@sentry[\\/]/`, priority 35, `includeDependenciesRecursively: false`). Confirm after the build that `@sentry/browser` sources are absent from the entry's sourcemap and the entry has a dynamic import for the Sentry chunk.
2. Edge 1a: `portals/$portalId.tsx:6-10` → `PORTAL_DETAIL_TABS`/`PortalDetailTab` from `#/components/features/portal/portal-detail/portal-detail-rules`, `PortalDetailPage` from `…/portal-detail/portal-detail-page`. Edge 2a: `import-google/$importId.tsx:4-7` → `googleImportStatusQuery` from `…/google-import-manager/google-import-queries`. Edge 5: extract `peopleSearchSchema` (`people-page.tsx:21`) into `…/people/people-search-schema.ts`; `people.tsx:16-19,76` imports the leaf. Edge 6: `dashboard.tsx:24-29,91-92` `pendingComponent`/`errorComponent` → a new leaf `…/dashboard/fleet-overview-fallbacks.tsx` holding only the loading/error views; same class at `portals/{$portalId:88-90,index:58-59,new:35-36}` and `p/$token.tsx:128-129` — apply the same split where the fallback lives in a heavy module. Edge 7: `settings/ai.tsx:14,37` — return the notice from the server fn instead of importing `MERCHANT_AI_NOTICE` client-side. Edge 8: `AI_PRIMARY_CATEGORIES` → new leaf `src/shared/ai-primary-categories.ts`; `openai-route-output-schemas.ts` imports it; repoint the 5 consumers (`inbox-search-schema.ts:6`, `inbox.dto.ts:7`, `ai-review-insights.port.ts:2`, `ai-category-labels.ts:11`, `route-preparer.ts:48`); `openai-route-output-schemas.test.ts` keeps every assertion, only the import moves.
3. Delete `src/shared/config/bundle-budgets.ts` + `.test.ts` (sole importer is the test; the gate is `scripts/check-bundle-budget.mjs`; its catalogue row `:3832` stays as the script's row).
4. `scripts/check-bundle-budget.mjs`: after the cuts, in the same commit, re-set `initialClosureGzip` to measured + ~3 % (expected ≈ 356,000 → lower if Sentry deferral lands: measured), `mainEntryGzip` to measured + 2 % (expected ≈ 90,000), keep `lazyChunkGzip` 128,000 (`vendor-charts` 92,640 becomes the largest lazy chunk); rewrite the header table with the 2026-09-08 measurement and the two-mechanism diagnosis; state the target as the measured floor (≈ 330,000 / ≈ 303,000 with Sentry deferred), not 204,800.

**Verification.** `pnpm build && pnpm check:bundles` green at the new ratchets; record entry, closure and chunk count before/after; `pnpm test:unit -- src/components src/shared src/routes` green; `pnpm test:storybook` green if any story-imported component file was split; live: dashboard, portal detail (all four tabs), people page, Google import wizard, `/settings/ai` render on the dev server with no console error.

## WP5.2a — Dead ceremony: one-off scripts, orphaned governance modules, e2e self-proofs, the Storybook console gate

**Anchors (scoutceremony.md §1, §3, §4).** 37 non-test `scripts/ops/*.ts`, 33 in the dispatcher. **KEEP 8** (`reconcile-publication` — runbooks §6 —, `rebuild-projection`, `rebuild-metric-projection`, `recover-recent-activity`, `repair-partial-offboarding`, `report-capability-refusal`, `report-organization-lifecycle`, and everything not in the DELETE list). **DELETE 9**: `reconcile-recent-activity-vocabulary` (already unreachable — not in the dispatcher), `report-portal-access-artifacts`, `report-legacy-custom-roles`, `report-legacy-multi-org`, `report-legacy-guest-compatibility`, `report-compatibility-read-surfaces`, `report-non-fk-references`, `report-inbox-handling-cutover`, `report-portal-beta-readiness`, `report-guest-response-readiness`. The reason is **"one-off cutover / contraction inventory for a migration the program struck"**, not "subject gone" — every table they read still exists; say that in the PR. No runbook §1–21 names any of them (the runbook tests pin section numbers, not commands — do not renumber sections).

**Steps.**

1. Delete the 9 scripts and their collateral (scoutceremony.md §1 "Collateral" table): `non-fk-reference-surfaces.ts` (405) + `non-fk-reference-scanner.repository.ts` (114) + its integration test; `legacy-reconciliation-inventories.ts` (336) + tests; `guest/application/compatibility-read-inventory.ts` + repository + tests; `inbox/application/inbox-handling-cutover.ts` + repository + tests + the `canonicalInboxHandlingCutoverReport` export from inbox `public-api.ts`; `guest/application/guest-response-reconciliation.ts` + repository + tests; `portal/application/portal-beta-readiness-reconciliation.ts` + repository + tests; `feed/application/use-cases/reconcile-recent-activity-vocabulary.ts` + `recent-activity-vocabulary-invocation.ts` + the store + port + `feed/build.ts:33-41` wiring + tests. `contraction-inventory-registry.ts` (177): delete its two rows (`:79-87`, `:88-93`); if nothing remains but scaffolding, delete the module and the two retention-test cases that read it (`retention-registry.test.ts:9,168`, `retention-registry.integration.test.ts:17,239` — their subject "compatibility mirrors are untouchable" has no mirror left); otherwise keep what remains and say what it still records. `.fallowrc.json:21`. Catalogue: delete the 9 `ops(` rows (`:3588-3758` range, by name) and the stale row `:4084-4092` (`identity-invitation-fact-contract.ts` does not exist). `scripts/ops/index.ts` COMMANDS. Docs: `operator-commands.md:30,31`, rewrite `operator-reports.md` to the surviving reports only (if none survive, delete it and its README/BETA mentions), `retention-registry.md:78,176-189,213-217`.
2. e2e: delete `e2e/deployed/closed-beta-deployed-probes.spec.ts`, `deployed-target.ts`, `deployed-target.test.ts`, `playwright.config.ts:5,113-117,125` (no runner, no CI job, consumer archived). `e2e/error-detection.spec.ts` (404): keep only the cases that inject their own fixture and assert the harness detects it (pageerror, unhandled rejection, console.error, non-2xx server fn, net failure, the gated-test proof); delete every case whose subject is the shipped `ERROR_ALLOWLIST` (empty today — `error-allowlist.ts:63`) or the fetch-abort correlation's negative controls; record the before/after line count.
3. Storybook console gate: `vitest.config.ts` storybook project gets `setupFiles: ['./.storybook/vitest.setup.ts']`; the setup file installs a `beforeEach` spy on `console.error` and an `afterEach` that restores it and **fails the story** on any message not in an allowlist (owner + reason + expiry, the `e2e/helpers/error-allowlist.ts:30-61` shape, starting empty). Run `pnpm test:storybook`; fix every story or component that logs (prefer the fix; allowlist only with a reason); delete the "Known gap" comment at `.storybook/main.ts:24-26`.

**Verification.** `pnpm typecheck && pnpm lint:ci && pnpm test:unit` green; `pnpm test:integration -- src/shared/db/retention src/contexts/guest src/contexts/inbox src/contexts/portal` green; `pnpm ops` prints 24 commands; `pnpm test:storybook` green with the gate live (prove it once with a throwaway `console.error` in a story → fails → revert); `pnpm test:e2e --project=full --grep "error detection"` green locally if the stack is up, else CI's `full` shard; `git grep -n "report-legacy\|non-fk-reference\|inbox-handling-cutover\|compatibility-read\|deployed-target\|DEPLOYED_CRITICAL" -- src scripts e2e docs ':!docs/archive'` → none.

## WP5.2b — Dead ceremony: the unreachable hero-image path

**Anchors (scoutceremony.md §2).** Keep the column `portals.hero_image_url` and its read path (DTOs, `public-portal-content.tsx:119-124`, previews — writes are already pinned to `null`). **Keep the storage stack**: `StoragePort` (arbitrary-key half, `storage.port.ts:45-62`), `createS3StorageAdapter`, `AWS_S3_*`, `compose.local.yml` `media` profile, runbooks §20 — Identity avatar and organization-logo uploads are live through `container.assetStorage`. Delete the issued-upload half only.

**Steps.** Delete `process-image.job.ts` + test, `cleanup-upload-sources.job.ts` + test, `portal-upload-issuance-store.ts` + integration test, `portal-upload-issuance-store.port.ts`, `domain/upload-issuance.ts`, `in-memory-portal-upload-issuance-store.ts`, the `IssuedPortalUploadStoragePort`/`PortalUploadDerivative`/`PortalStoragePort` half of `storage.port.ts` and of `s3-storage.adapter.ts` (+ its test arms), the `portal.hero_image.*` events (`portal/domain/events.ts:242-266,890-911`, `schema-registrations.ts:1746-1755`), the outbox consumers (`portal/infrastructure/outbox-consumers.ts:32-60` + test, `feed/infrastructure/activity-outbox-consumers.ts:83-84,693-697` + test cases), registrations (`bootstrap.ts:18-20,76-78,277-283,287-299`, `portal/build.ts:77,88,152`, `composition.ts:655-657` `portalWorkerRuntime`), catalogue rows (entry-point `:2607-2632,2971-2972,3273-3274`; event-job `:1389-1420,2175-2190,2378-2392`), `tenant-predicate-exemptions.ts:196-201`, `readiness.test.ts:109-113`, the schema table `portalUploadIssuances` (`portal.schema.ts:751-859`) with its `data-fate-authority.ts` row, `schema/index.ts` export and `migration-verification.test.ts` name; `RETIRED_SCHEDULER_JOB_NAMES` += `'portal-upload-source-cleanup'` in the same commit (and delete its contract from `JOB_OPERATIONAL_CONTRACTS` — `operational-catalogue.ts:148-153` throws otherwise); `docs/operations/backup-and-lifecycle.md:202-211`; `portal/CONTEXT.md:47,63`. Then `pnpm db:baseline && pnpm db:reset && pnpm check:schema-drift "$DATABASE_URL"`.

**Verification.** `pnpm typecheck && pnpm lint:ci && pnpm test:unit && pnpm test:integration` green; schema drift clean; `git grep -n "hero_image\.\|upload-issuance\|uploadIssuance\|IssuedPortalUpload\|process-image\|portal-upload-source-cleanup" -- src scripts docs ':!docs/archive'` → only the tombstone and the column's read path; the worker boots and its scheduler reconciliation removes `portal-upload-source-cleanup-recurring` from a Redis that had it; live: a published portal with `hero_image_url = null` renders, avatar upload on `/settings/profile` still issues a presigned URL.

## WP5.3a — The container seam (ARCH row 7)

**Anchors (scoutseam.md §1–5).** 9 files / 1,105 lines: `composition/deployables.ts` (196), `container-partition.ts` (147), `deployables.test.ts` (295), `process-fixtures/*` (6 files, 467). Production consumers: `src/worker/index.ts:23,117`, `src/composition.ts:85-90,803-825`, `scripts/ops/operator-command.ts:24,27`, `src/bootstrap.ts:8,128,593` (type only). ADR 0018 does not exist (merged into 0019, which already prescribes `createContainer(overrides)`). `createOperatorContainer` and its refusals are **controls — keep**; the key projection is ceremony.

**Steps (the design in scoutseam.md §5, binding).** (1) Move the process claim into `src/composition.ts` (~25 lines: `claimProcessContainer(kind)`, `releaseProcessContainer()`, `DUPLICATE_CONTAINER_ERROR`), called from `getContainer()`, `closeContainer()`, the worker and the operator harness. (2) New `src/composition/operator-container.ts` (~65 lines: `operatorGraphOptions`, `withOperatorProviderRefusals`, `createOperatorContainer` returning `Container`) — a separate module so the `deployable-containers` eslint category and the four boundary controls keep a subject; retarget `eslint.config.js:181,438-445` and `scripts/check-architecture-boundary-controls.mjs:117-131,203-204` at it and delete the `top-level, test-helpers` allowance whose only beneficiary was `process-fixtures`. (3) Delete the partition and `deployables.ts`; `getContainer(): Container`; `bootstrap(container: Container)`; `src/worker/index.ts:117` → `claimProcessContainer('worker'); createContainer({ enableJobs: true })`. (4) Delete `deployables.test.ts`, `one-container-per-process.test.ts`, the six fixtures. (5) Replacement `src/shared/architecture/one-container-per-process.test.ts` (in-process, no `spawnSync`): claim/refusal/rebuild lifted from `deployables.test.ts:256-295`; "`#/bootstrap` is imported by exactly `src/worker/index.ts`" via the `walk` helper (`#/shared/testing/source-tree`); determinism case in `outbox-consumer-registration.test.ts` (build twice, compare sorted registry keys); simulation policy-binding case in `process-policy-binding.test.ts` (`createSimulationContainer` claims too — closes the hole the fixture only documented).

**Verification.** `pnpm typecheck && pnpm lint:ci` green; `node scripts/check-architecture-boundary-controls.mjs` → still `20 invalid imports rejected; 11 valid seams accepted` (retargeted); `pnpm test:unit` green and the unit wall recorded; the worker boots (`BullMQ worker started on default queue`), a second `createContainer` in the same process throws the named error (the new test), `pnpm ops report-organization-lifecycle --help` runs through the operator container; `git grep -n "deployables\|container-partition\|process-fixtures\|WorkerContainer\|WebContainer\|projectContainer" -- src scripts eslint.config.js` → none.

## WP5.3b — Catalogue slimming (ADR row 7, by deletion) — wave 2

**Anchors (scoutseam.md §6–8).** `entry-point-catalogue.ts` 4,228 + `event-job-catalogue.ts` 2,716 = 6,944 hand lines; both guard tests already deleted (headers still advertise them — fix). Runtime readers filter to `job|consumer|schedule` (`system-execution-policy.ts:35-36`, `delayed-execution-gate.ts:124-125`): the `server_function`, `route_ui`, `route_api` and `operator_command` row kinds — **2,806 lines (66 %)** — have no runtime reader. Of `EntryPointRow`'s 18 fields only `kind`, `action`, `capability`, `resourceScope`, `externalEffect`, `name` are read. Of `EventFamilyRow`'s 20 fields only `eventType` and `consumers` are read (the rest only through `beta-notification-trigger-matrix.ts`, which has no production caller). `JobFamilyRow` is real except `notes`, `region`, `retention` (constants). `notes:` = 581 + 209 lines.

**Steps.** Before deleting a row kind, prove the absence of readers by command (`git grep -n "OPERATOR_ROWS\|ROUTE_UI_ROWS\|ROUTE_API_ROWS\|SERVER_FUNCTION_ROWS\|operator_command\|route_ui\|route_api\|server_function" -- src scripts ':!*.test.ts' ':!src/shared/governance/entry-point-catalogue.ts'`; `scripts/ops/operator-command.ts` in particular — if it reads operator rows, keep those rows' live fields). Then: delete the 12 decorative `EntryPointRow` fields and the 4 inert row kinds; delete the 13 decorative `EventFamilyRow` fields, `beta-notification-trigger-matrix.ts` + test; delete `notes`/`region`/`retention` from `JobFamilyRow` (inline the two constants where read: `operational-catalogue.ts:106-107`); rewrite the constructors; a script (node/python, kept out of the tree) does the mechanical edit, Prettier formats. Tests: delete `operational-action-history-entry.test.ts` (subject inert), rewrite `portal-atomic-lifecycle.test.ts:47-63`, `event-adapter.test.ts:336-338`, `recent-activity-identifiers.test.ts:66-67`, `outbox-consumer-registration.test.ts` `.file` assertion; keep every test on live fields. Fix the two headers.

**Verification.** `wc -l` both files recorded (expected ≈ 1,450 total); `pnpm typecheck && pnpm lint:ci && pnpm test:unit` green; the worker boots and `delayed-execution-gate`/`system-execution-policy` behave identically (their tests); `git grep -n "guard test\|event-job-catalogue.test" src/shared/governance` → none.

## Decided without code, recorded in the close

- **SEC row 9 — struck.** The two `/api/auth` limiters are not duplicates: outer 60 req / 60 s fixed window per IP across all auth POSTs (`composition/infrastructure.ts:28-32`, fail-closed), inner Better Auth 3 req / 10 s rolling window per IP per endpoint (`better-auth-rate-limit-storage.ts`); deleting the inner storage downgrades it to per-replica process memory (`auth.ts:143-146`). Both stay.
- **DEPS row 10 — struck for the checker.** `check-product-state-consistency.ts` reads its ledger + `query-keys.ts` (not `capability-fate.ts`/`BETA.md`), enforces five rule families of which unit tests cover one; its 405-line ledger is a live ratchet. It stays; `check-test-quality.mjs` and `check-architecture-boundary-controls.mjs` are load-bearing controls (the latter proved WP4.1). SEC row 12 (`OPS_OPERATOR_IDENTITIES`) stays: a fail-closed operator allowlist is a control.

## Close

Record in `LEAN_TRANSFORMATION_PLAN.md` § "Phase 5 progress" per WP the measured delta and every correction above that held or did not; § "Phase 5 close" with: entry/closure gzip and chunk count, `scripts/ops` count, catalogue lines, seam lines, docs/legal status, tracked files, TS lines, unit wall, CI wall; the plan-review findings and fixes; the browser-verification matrix (capability → evidence).
