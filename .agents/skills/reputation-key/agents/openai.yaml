interface:
  display_name: "Reputation Key"
  short_description: "Repo-specific patterns and workflows for reputation-key"
  default_prompt: "Use the reputation-key repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true