# Lean transformation — program roadmap with Phase 0 and Phase 1 execution specs

## Context

`review/2026-09-05-lean-audit/` (README + 8 audits, measured at `f3d9baac`; HEAD is now `a9d7ffe1`, three `ops` commits later) concludes that the product rules are right and the self-imposed machinery around them is the cost, and lays out a five-phase program to remove ~30% of TypeScript, all generated JSON, 182 migrations, five sidecars and most governance. This plan turns that program into ordered, PR-sized work packages (WP). Phase 0 and Phase 1 are written to implementer-executable detail against the current tree (every anchor below was read this session; where the audit's anchor was wrong, the corrected one is used). Phases 2–4 are sequenced with entry criteria and get their own detailed plan when reached.

Baseline measured 2026-09-05 at `a9d7ffe1` on the pinned runtime (`fnm use`, node 22.23.2): `pnpm typecheck` green in 56 s; `pnpm lint:ci` green; `pnpm test:unit` **1,307 files, 12,791 passed, 5 skipped, 86–182 s wall across four runs** (86 s on an otherwise idle machine; setup, not test execution, dominates the spread — re-measure idle before WP1.2 records before/after); 5,081 tracked files; 806,737 TS lines; 182 migrations; 2.96 M lines of `drizzle/meta`; governance 23,286 / release 17,877 / scripts/release 18,666 / `.railway` 1,952 lines. The audit's 616 s local unit-suite figure was measured on node 26 (drifted runtime); the four files it names as 51% of execution run in 14.7 s on 22.23.2. Running the pinned node is worth more to the local loop than any deletion in this plan. Tag `lean-baseline` marks `a9d7ffe1` as the rollback point.

Settled decisions (from the user, 2026-09-05):

- **The app is stopped and the database is disposable.** No data preservation, no dumps, no journal-reset SQL: the schema is regenerated from `pgTable` declarations into a fresh baseline whenever it changes, and every environment starts from an empty database.
- **Railway is out of scope.** Only repository code/structure changes. Railway-side defects in the audit (backups, public proxies, alert env vars, sandbox services) are not planned; their repo-side residue is deleted where it is code.
- **Commits go directly to `main`.** No PRs. The local gate below runs before every commit; CI on `main` is a post-hoc signal, fixed forward before the next WP starts.
- **Storybook stays with one runner** (`@storybook/test-runner`, the `pnpm test-storybook` a11y gate); `addon-vitest`, `addon-mcp`, the vitest browser project and the build-only CI job go in Phase 3 (user-overridable, see Assumptions).
- **Work happens on branch `lean`** in `~/kodes/projects/rep-key` — a linked worktree whose local `main` is checked out in `~/conductor/repos/reputation-key` and 528 commits behind. One commit per WP; `git push origin lean:main` fast-forwards the remote. A tag per phase boundary is the only rollback mechanism, since there is no review.
- **The 266 local branches are abandoned** except an approved cherry-pick list. Scan at `a9d7ffe1`: 208 carry commits off `main`, only 14 diverged less than 100 commits back, and by patch-id only 4 hold unmerged content — three of them the Google approval-ceremony work WP2.2 deletes. Salvage: `c46e86cad` (performance panel fails closed in silence — product bug in `contexts/integration`, which survives the program) and `a54cb4aa4` (withhold unverified Google locations from the import list); optionally `e7ba9de41` (make `permit-start-deadline-backfill.ts --batch` real). Nothing older is rebasable onto the post-Phase-1 tree.
- **The pinned node is mandatory** (`.nvmrc` 22.23.2). On a drifted runtime the fence in `scripts/check-test-quality.mjs` retires ~146 governed AI-language tests and the suite still exits 0. Until WP1.2 deletes the fence, every gate runs under `fnm use`.
- **WP1.2 stays the full purge** (decided after the timing correction above): justified by maintenance cost and by unblocking the WP1.4–1.7 deletions, not by wall time.

End state after Phase 1 (verifiable): `docs/BETA.md` is the only authority; `drizzle/` holds exactly two files; `team`/`badge`/`leaderboard` and their 17 tables do not exist; no release-ceremony, IaC, legal-registry or standards-matrix code exists; `src/shared/architecture` and `src/shared/governance` tests are a named keep-list; the Staff shell, legacy routes, `/register`, `/` landing, Closure Center and hero-upload pipeline are gone; `pnpm test` is the unit project only; Dependabot bumps are blocked by **no governance gate** — but see the correction below, two dependencies stay version-coupled by design.

## Program rules (apply to every WP)

- **Local gate before each commit:** `pnpm typecheck && pnpm lint && pnpm test:unit`. Add `pnpm test:integration` when `src/shared/db/**`, any `*.schema.ts`, `drizzle/`, or a repository/command store changed (needs local Postgres; `src/shared/testing/test-db-setup.ts` `ensureTestDatabase` bootstraps `test` role/db). Add `pnpm build && pnpm check:bundles` when `vite.config.ts`, `src/routes/**` or `src/components/**` changed. Add `pnpm test-storybook` when stories changed.
- **Failing-test policy:** a test that fails because it asserts prose, source text, a catalogue row, a migration file, a journal count/tag, or a file's existence for something this program deleted → **delete the test** (never re-pin). A test that fails because behaviour changed → fix the behaviour or the fixture. Never add `it.skip`, never add a skip-register entry.
- **No new governance:** no new catalogue rows, registries, evidence JSON, ADR-presence tests, or `*.md` mirrors of code. `docs/BETA.md` is the only place a rule is written down without a test.
- **Catalogue rows:** `src/shared/governance/entry-point-catalogue.ts` is production data (imported by `src/shared/auth/system-execution-policy.ts:26` and `src/shared/jobs/delayed-execution-gate.ts:37`; unknown entries fail closed, stale rows are inert). Its bidirectional test is deleted in WP1.2; after that, delete the row of every entry point you delete (rows are named per WP) but never add one.
- **Schema change loop (after WP1.3):** edit `src/shared/db/schema/*.ts` → `pnpm db:baseline` (regenerates the whole journal) → `pnpm db:reset` (drop + create + `db:migrate-deploy`) → `pnpm check:schema-drift`. `drizzle/` always contains exactly `0000_baseline.sql`, `0001_db_constructs.sql`, `0002_db_seed.sql`, one `meta/NNNN_snapshot.json` per migration, and `meta/_journal.json`.
- **Commit style:** existing conventional commits (`chore(deps): …`, `refactor(governance): …`, `docs(beta): …`); body names the WP id (e.g. `WP1.3`).
- **Order:** WPs within a phase run in the listed order unless marked independent; each leaves the tree green under the local gate.

## Phase 0 — Stop the bleeding (code-only; ~1 day)

Railway-side defects 1, 2, 3, 8 of the audit's §3 are out of scope (user decision). Defect 3 needs no code: `worker.job-runtime-unready` (`src/shared/observability/alert-definitions.ts:306-333`) already fires on `snapshot.jobs.ready === false` and prints `deadLetters`; only the unset `ALERT_WEBHOOK_URL`/`SENTRY_DSN` (`src/shared/config/env.ts:112,144`, both `.optional()`) keep it silent. Defect 7 (AI notice says "at most one hour", request contract sets `prompt_cache_retention: '24h'`) is deferred to Phase 3 WP3.3: `src/shared/ai-openai-request-contract.ts:13-15` records that `gpt-5.6-luna` rejects `in_memory` with HTTP 400, so only the notice can change, and the notice digest `MERCHANT_AI_NOTICE_DIGEST` (`src/shared/merchant-ai-notice-contract.ts:176-177`) is embedded as CHECK-constraint literals in the AI catalogue migrations (`drizzle/0046`, `0061`, `0062`, `0067:209`) that WP3.3 turns into TypeScript constants. Defect 6 (`sharp` missing in prod) is resolved by deleting the hero-image pipeline in WP1.7 (it serves `portal.upload`, fate `safety_blocked`), not by promoting `sharp`.

### WP0.1 — Unblock dependency updates (CI.md row 1)

Delete the three exact-version authorities and the pin test; keep `pnpm-lock.yaml`, `check:dependency-audit`, `check:action-pins` (`scripts/check-action-pins.mjs` is JSON-free and needs no edit).

1. Delete files: `security/technology-stack.json`, `scripts/ci/check-technology-stack.ts` (1,009 lines), `scripts/ci/check-technology-stack.test.ts`, `scripts/ci/check-technology-stack-authority-shape.test.ts`, `docs/operations/technology-stack-authority.md`.
2. Edit: `package.json:124` remove script `check:technology-stack`; `.github/workflows/ci.yml:60-61` remove step `Technology stack authority and reproducibility`; `scripts/ci/operational-tooling-coverage.test.ts:72-97` remove the four `ruleSeverity(...)` assertions naming the two deleted files; `src/shared/governance/entry-point-catalogue.ts:5563-5571` remove the `ops('scripts/ci/check-technology-stack.ts', …)` row; `src/shared/governance/operator-command-mutation-classifier.ts:65` remove that path; `src/shared/CONTEXT.md:129-136` remove the "run `pnpm check:technology-stack` …" paragraph.
3. Delete the SDK byte/version attestation gate, which fails on any `openai`/`undici` bump: `scripts/check-ai-contract-attestations.ts` (`PINNED_PROVIDER_DEPENDENCIES` at :16-19, CLI at :518) and `src/shared/ai-canonicalizer-attestations.test.ts`; remove `check:ai-contract-attestations` from `lint:ci` (`package.json:30`) and its own script entry; remove its `entry-point-catalogue.ts` row (grep `check-ai-contract-attestations`).
4. Leave version specifiers in `package.json` as they are (exact or caret); Dependabot rewrites them.
5. Grep proof: `git grep -n "technology-stack\|check-ai-contract-attestations" -- ':!docs/release-evidence' ':!docs/archive' ':!review'` returns only `docs/operations/gate-inventory-2026-08-31.md` (archived in WP1.1).

Verification: `pnpm lint:ci` green. Throwaway proof: bump one dependency's version in `package.json` (e.g. `@tanstack/react-query` patch) → `pnpm install` → `pnpm lint:ci` still green → `git checkout package.json pnpm-lock.yaml`.

**Correction, measured 2026-09-05 by actually taking the updates.** "Dependabot bumps are not blocked by any version pin" is false as written. WP0.1 removed the _governance_ gates, and 22 of 24 patch/minor bumps then landed green in one commit. Two did not, and both are correct to refuse:

- **`openai`** — the SDK version is part of the attested request contract in _production code_, not ceremony: `services/ai-egress-gateway/openai-connector.ts:163,167` hardcodes the SDK's own `user-agent` and `x-stainless-package-version` as `7.4.0` because the exact request bytes are attested, and `src/shared/ai-openai-provider-profile.ts:192` + `src/shared/ai-gateway-build-attestation.ts:11` repeat it. Installing 7.10.0 makes the real SDK send a different version than the attestation expects, so every dispatch fails closed with `no_dispatch` (14 connector tests prove it). Bumping `openai` means re-issuing that attestation. Defer to **WP2.3**, which deletes `services/ai-egress-gateway` outright — fixing the coupling first would be throwaway work.
- **`opencc-js`** — `src/shared/ai-language-generated-artifacts.test.ts` digests the imported OpenCC closure members together with the table generated by `scripts/generate-ai-zh-orthography-table.ts` (manifest `src/shared/ai-zh-orthography-profile-v1.manifest.json`). A bump changes the library bytes, so the artifact must be regenerated and re-attested — a governed AI-language change, not a version bump. Defer to a dedicated change.
  Also corrected in passing: `src/shared/architecture/error-monitoring-wiring.test.ts` pinned `@sentry/node` and `@sentry/tanstackstart-react` to the literal `10.71.0`, which forced a re-pin on every Sentry release while proving nothing. It now asserts the real invariant — the two packages carry the _same_ exact version, the wrapper stays a devDependency, the Node SDK stays a dependency — and the 10.73.0 bump landed without touching it. Two further version pins that WP0.1 missed because they live in the unit suite rather than `lint:ci`: `src/shared/architecture/context-acceptance-matrix.test.ts:585` (`openai` exact) and `src/contexts/ai/domain/catalogues/operation-profiles.test.ts:80` / `src/shared/ai-openai-provider-profile.test.ts:18` (`sdkVersion`). They are all downstream of the `openai` attestation above, so they resolve when WP2.3 does.

### WP0.2 — Make every page ship a truthful bundle (UI.md row 1)

Corrected anchors: `vite.config.ts` is 226 lines; the rolldown `codeSplitting.groups` block is at :74-95 (`vendor-charts` :76-80, `vendor-dnd` :81-85, `app-shared` :86-94). `src/components/inbox/inbox-search-schema.ts` already exists (64 lines); `inbox-page-v2.tsx:34` merely re-exports it.

1. `vite.config.ts:75`: insert as the first group `{ name: 'vendor-react', test: /node_modules[\\/](?:react|react-dom|scheduler)[\\/]/, priority: 40 }` so React never lands in `vendor-charts`.
2. `src/routes/_authenticated/inbox/index.tsx:6`: replace `import { InboxPageV2, inboxSearchSchema } from '#/components/inbox/inbox-page-v2'` with two imports — `InboxPageV2` from `#/components/inbox/inbox-page-v2` and `inboxSearchSchema` from `#/components/inbox/inbox-search-schema`. Delete the re-export at `src/components/inbox/inbox-page-v2.tsx:34`; fix any other importer of the re-export (`git grep -n "inboxSearchSchema.*inbox-page-v2"` → expected none besides the route).
3. `scripts/check-bundle-budget.mjs`: replace the "lazy = every non-entry chunk" model with a static-closure measurement. Parse every `.output/public/assets/*.js` for static specifiers — the minified forms `from"./x.js"`, `from'./x.js'`, `import"./x.js"`, `export{…}from"./x.js"`; ignore `import("./x.js")` — walk from the single `index-*.js` entry, and report `initialClosureGzip` = gzip of every reachable chunk + all CSS. Budgets: keep `mainEntryGzip` 130 KiB; replace `initialPayloadGzip` with `initialClosureGzip` **204,800 B (200 KiB)**; keep `lazyChunkGzip` 125 KiB for chunks outside the closure. Print the closure members sorted by size on failure. Rewrite the header comment (:2-19) with the 2026-09 measurement.
4. **Executed 2026-09-05; steps 1–3 landed, step 4 is the open work.** Measured closure after steps 1–2: **679,134 B gzip over 174 JS chunks + 1 CSS** — 3.3× the 200 KiB target and 83% of all JS in the build. Verified empirically: a static page importing only the entry chunk makes the browser fetch exactly 174 chunks (660,906 B gzip), matching the script's walk. The 200 KiB budget was calibrated against the old gate's false "initial payload = entry + CSS = 137,746 B", so it was never a measurement of this.
   Diagnosis: chunk grouping is not the cause and route splitting already works — 43 output chunks carry `tsr-split`, and the installed router (1.170.30) / start (1.168.47) expose no `autoCodeSplitting` knob to turn on. The cause is **53 static edges where a route's non-component code imports a component barrel at module scope**: `validateSearch`, `beforeLoad`, loaders and the sibling `-*.ts` / `-*-fns.ts` modules are not split out, so whatever they import is pinned into first paint together with its npm dependencies. Step 2's inbox fix is one instance of ten. Ranked by the bytes each edge pins (gzip):
   - 38,415 B — `properties/$propertyId/portals/{index,$portalId,new}.tsx` + `-portal-detail-data.ts`
   - 33,515 B — `inbox/index.tsx` (fixed), `properties/$propertyId/reviews.tsx`, `-inbox-fns.ts`, `inbox/-assignment-candidates.ts`
   - 18,449 B — `settings/integrations.tsx`, `properties/import-google/{index,$importId}.tsx`
   - 9,253 B — `-notification-fns.ts`, `settings/notifications.tsx`
   - 6,224 B — `properties/$propertyId/people.tsx`, `home.tsx` (deleted in WP1.7), `settings/ai.tsx`
   - 3,174 B + 3,020 B — `dashboard.tsx`, `properties/$propertyId/index.tsx`
   - 4,230 B + 2,274 B + 2,001 B + 1,810 B — pinned by the entry and `_authenticated.tsx` (profile/security/notifications/reviews barrels)
   - 1,853 B — `properties/$propertyId/goals/{index,$goalId,new}.tsx`
     Fix pattern per cluster: move the value the non-component code needs into a leaf module and import that (the `inbox-search-schema.ts` shape), never the barrel. Cutting all ten removes ~165 KB of component chunks plus the npm/vendor chunks that leave with them (`vendor-charts` 93 KB and `vendor-dnd` 14 KB are only reachable through chart/dnd components).
     Floor check before promising 200 KiB: entry 37,896 + `vendor-react` 59,070 + route modules 72,135 + shared/contexts 48,735 + CSS 21,297 = **239,133 B** with every addressable edge cut. **200 KiB is unreachable without also shrinking the shell**, so treat the target as a follow-up WP with its own measurement, not a Phase 0 acceptance criterion.
5. `.github/workflows/ci.yml:342-343` (`Bundle budget` step) unchanged.
6. `includeDependenciesRecursively: false` added to `vendor-react`, `vendor-charts` and `vendor-dnd`: without it recharts' transitive deps (`clsx`, `use-sync-external-store`, redux, es-toolkit) joined the chart group, so a chunk needing only `clsx` statically imported all 117 KB of it. This moved 24 KB out of `vendor-charts` but not out of the closure — the deps simply re-chunked inside it.

Verification (recorded): `pnpm build && pnpm check:bundles` green against the **ratchet** `initialClosureGzip` 689,152 B (673 KiB), the measured actual + 1%; `mainEntryGzip` 37,569 B / 133,120 B; largest chunk outside the closure 29,256 B / 128,000 B. The plan's browser check (`pnpm dev` → devtools JS total < 250 KB) is not performable as written: `pnpm dev` serves unbundled modules (231 requests, 19 MB), and the production server refuses to boot locally without the deploy environment. `/login` was rendered in a real browser against `pnpm dev` (sign-in form present, no console errors) as the smoke check, and the payload number comes from the built assets instead.

### WP0.3 — One Docker image (DEPS.md rows 2–3)

Corrected: `railway.worker.json` (12 lines) has no `startCommand`/`preDeployCommand`; the worker start is `Dockerfile.worker:96` `CMD ["node","--import","./dist-worker/worker-observability-preload.js","dist-worker/index.js"]`. `Dockerfile:157-158` already copies `.output` and `dist-worker` into the web image.

1. Delete `Dockerfile.worker` (and `Dockerfile.worker.dockerignore` if present).
2. `railway.worker.json`: `"dockerfilePath": "Dockerfile"` and add `"startCommand": "node --import ./dist-worker/worker-observability-preload.js dist-worker/index.js"`.
3. `.github/workflows/ci.yml:450` worker matrix entry: `"dockerfile":"Dockerfile"`. The worker smoke contract for that image (in the smoke section `ci.yml:567-640`, exact lines unverified — confirm first) must run the container with the worker command above instead of the image `CMD`; if the smoke harness cannot override the command, remove the `worker` entry from the matrix (the web image _is_ the worker image) and let the digest map publish one image.
4. `src/shared/testing/pinned-runtime.test.ts:107-127`: remove `Dockerfile.worker` from the Dockerfile list (the whole test is deleted in WP1.2; edit now so the gate stays green).
5. `git grep -n "Dockerfile.worker"` → fix every remaining hit (expected: `security/container-images.json`, `.railway/*.ts` and tests, `docs/operations/*.md`, `README.md`): code/config/tests → update to `Dockerfile`; docs → one-sentence fix.
6. ~~Move `@sentry/tanstackstart-react` to `dependencies`~~ — **wrong, it stays a devDependency.** Being imported by `src/start.ts` is exactly why: the wrapper is _bundled_, and `src/shared/architecture/error-monitoring-wiring.test.ts:16-18` plus the `vite.config.ts` comment at :124-126 both record the decision — externalizing it would ship its source-map uploader and `@sentry/cli` into production dependencies. Only `@sentry/node` is external. Deleted `@types/sharp` instead (sharp ships its own types via `./dist/index.d.mts`); `sharp` itself stays — it is dynamically imported at `src/contexts/portal/infrastructure/jobs/process-image.job.ts:61` and goes with the hero-image pipeline in WP1.7.
7. **Executed 2026-09-05.** Extra work the plan did not anticipate: `scripts/ci/check-container-image-policy.ts` rejected the inventory with `duplicate dockerfile` once `web` and `worker` pointed at the same file, so `dockerfile` was dropped from its uniqueness set (`id`, `ciImage`, `releaseRole` still unique) — one image legitimately backs several release roles. `compose.local.yml`'s worker service had no `command:` and relied on the image CMD, so it now carries the worker start command explicitly. The worker CMD assertion in `error-monitoring-wiring.test.ts` moved from the deleted Dockerfile to `railway.worker.json.deploy.startCommand` + `compose.local.yml`, which is now the only thing guaranteeing the worker boots with its observability preload. The CI smoke harness _can_ override the container command (every worker assertion already uses `--entrypoint`), so the matrix contingency was not needed; a `require.resolve` check on both worker entrypoints was added to the worker smoke case.

Verification (recorded): `docker build -t repkey:lean .` succeeded in 80 s; `docker run --rm --entrypoint ls repkey:lean dist-worker/index.js dist-worker/worker-observability-preload.js .output/server/index.mjs` listed all three; both worker entrypoints `require.resolve` inside the image; image CMD is still the web server; labels `com.repkey.google-import-contract=final`, `com.repkey.rollout-scope=serving-final`, user `node` — the three the CI worker smoke asserts. `pnpm check:container-images` OK (10 Dockerfiles, 8 promoted, 2 CI-only); local gate green.

### WP0.4 — Hygiene (verified subset of README §7 Phase 0.2)

The audit's "24 zero-reference scripts" and "six dead devDependencies" are partly wrong: `build:local-tools` (`Dockerfile:117`), `build:google-egress-gateway-local` (`Dockerfile.google-egress-gateway:26`), `ops:reconcile-recent-activity-vocabulary` (`docs/operations/runbooks.md:60`), `dotenv-cli` (`package.json:63,66`) and `concurrently` (`dev:all`) are live; `security/audit-exceptions.json` is read by `scripts/check-dependency-audit.mjs:82` (exit 2 if missing) — keep it. `drizzle/meta` is not pruned here: three tests read old snapshots and the whole directory is regenerated in WP1.3.

1. `package.json` scripts — delete the eleven with zero references anywhere: `e2e:stack:smoke`, `local:stack:up`, `local:stack:smoke`, `local:stack:logs`, `local:stack:down`, `local:beta:up`, `perf:seed-fleet`, `beta:create-pre-cutover-dump`, `release:google-import-drill`, `generate:google-provider-fixtures`, `generate:ai-governance-artifacts`. Keep `dev:all`, `preview`, `lint:fix`, `test:watch` (developer entry points). Add `"clean": "rm -rf .output dist dist-worker dist-local-tools dist-provider-services storybook-static coverage test-results"`.
2. devDependencies — delete **only** `@vitest/coverage-istanbul` (`vitest.config.ts:59` uses `v8`); run `pnpm install --no-frozen-lockfile`. **Executed 2026-09-05 and corrected:** the other two named devDependencies are live. `eslint-import-resolver-typescript` plus the `settings['import/resolver']` block in `eslint.config.js` are what `eslint-plugin-boundaries` resolves `#/` aliases through — deleting them made `scripts/check-architecture-boundary-controls.mjs` reject three valid seams (positive control: it must print `24 invalid imports rejected; 14 valid seams accepted`). `shadcn` backs the shadcn MCP server (`.mcp.json` `pnpm exec shadcn mcp`, `.codex/config.toml`), so it is a live CLI with no code importer; keep it and its `.fallowrc.json` ignore entry.
3. ~~Delete `src/shared/domain/result.ts`~~ — **wrong, keep it**: `src/shared/domain/slug.ts:5-6` and `src/shared/domain/index.ts:21` import it (the audit's "0 importers" missed the relative `./result` specifier). `neverthrow` stays until Phase 3 WP3.1 deletes its two test importers (`src/contexts/goal/infrastructure/event-handlers/on-portal-deleted.test.ts:6`, `on-portal-group-deleted.test.ts:9`).
4. Delete `findings/` (2 files) and `.dockerignore:15`; delete `better-auth_migrations/`, the `generate` subcommand of `scripts/better-auth-schema.ts` (:52-58), the `auth:generate` script (`package.json:61`), `.dockerignore:17`, and the sentence at `docs/auth-migrations.md:49`; delete `tsconfig.node.json` (referenced by nothing).
5. `rm -rf drizzle.bak dist dist-provider-services test-results` locally; remove `.gitignore:79` (`drizzle.bak/`), `.dockerignore:16`, and the special case at `scripts/ci/check-typescript-project-coverage.ts:28`.
6. Commit `AGENTS.md` if `git status` shows it untracked.

Verification: local gate; `git grep -n "auth:generate\|better-auth_migrations\|tsconfig.node.json\|findings/\|drizzle.bak" -- ':!docs/archive' ':!docs/release-evidence' ':!review'` returns nothing.

### WP0.5 — Branch protection (GitHub, one command)

Commits go straight to `main`. If `git push origin main` is rejected by required status checks, run `gh api -X DELETE repos/$(gh repo view --json nameWithOwner -q .nameWithOwner)/branches/main/protection`. Nothing else in GitHub changes; CodeQL/Fallow/Simulation workflows keep running on push until Phase 2 WP2.5 folds them into `nightly.yml`.

## Phase 1 — Replace the authority, delete the dead (weeks 1–2)

Order is load-bearing: WP1.1 defines what must survive; WP1.2 removes the source-text/catalogue tests that would otherwise fail on every later deletion; WP1.3 makes schema deletion a two-command loop; WP1.4–1.7 are deletions; WP1.8 pins the invariants the Phase 2 collapse must not break.

### WP1.1 — `docs/BETA.md` becomes the authority; program docs and ledgers go (ADR.md row 1, 8, 9, 11)

0. **Step 1 is done (2026-09-05): `docs/BETA.md` is written, reviewed and corrected — 114 lines.** Two spec clauses below are superseded by what shipped. §1 does **not** fix the scale: "one Organization, six Properties, one developer" was a snapshot, not a rule, so the page states what the code enforces instead — Organizations and Properties are operator-provisioned with no fixed count, self-service is off, and a user holds exactly one active Organization Membership. §4 does **not** claim the runtime derives its sets from the fate table, because it does not yet; that becomes true in WP1.6. §1 also drops the cell/region/country-count vocabulary entirely (see WP3.2). Steps 2–7 below are the remaining work and are unblocked once the page is accepted.
1. Write `docs/BETA.md` (≤120 lines) with exactly the ten sections of ADR.md § "The one-page beta contract": (1) what the beta is — one Organization, six Properties, one developer, `CURRENT_RELEASE_POSTURE = 'closed-beta'` (`src/shared/release/release-posture.ts:49`); (2) external obligations — copied from the Google written response (see step 2) and GDPR notice duty; (3) product contract — compress §3.1–3.7 of `docs/comprehensive-beta-implementation-program-2026-08-25.md:39-125` to ≤40 bullets; (4) capability authority — "`src/shared/governance/capability-fate.ts` is the single table; the runtime derives its sets from it (WP1.6)"; (5) enforced architecture rules, each with its test/lint path — context boundaries (`eslint.config.js` boundaries + `scripts/check-architecture-boundary-controls.mjs`), atomic state+fact (`src/shared/architecture/atomic-*-outbox.test.ts`), tenant predicate (`tenant-predicate-canary.test.ts`), server-only import protection (`browser-reachability.test.ts`, `vite.config.ts` deny list), no ambient clock (eslint), identifier-only facts (`content-free-facts.test.ts`), no auto-publish (`no-auto-publish.test.ts`); (6) release checklist — CI green on the `main` SHA; `pnpm db:migrate-deploy` applied; `/api/health/ready` 200; one authenticated journey (Google review → Inbox → Confirm & Publish → observed); backup verified within 30 days once a production database exists again; 24 h watch; (7) operating commitments — RPO ≤15 min, RTO ≤4 h, the seven stop conditions from `docs/adr/0038-*.md`; (8) deferred capabilities — the `beta_disabled`/`safety_blocked`/`legacy_blocked` rows of `capability-fate.ts` with their `activation` text; (9) precedence — external obligations → BETA.md → ADRs → `docs/standards.md` → code; `docs/archive/**` is provenance; (10) change log — one dated line: "2026-09: replaces the 42-package program as authority".
2. `git mv docs/product-readiness-program-2026-07/google-business-profile-ai-policy-response-2026-07-14.md docs/external/google/`, likewise `…-clarification.md` and `attachments/`; fix every link (`git grep -n "google-business-profile-ai-policy-response"` — includes `docs/adr/0031-*.md`, `src/shared/governance/protected-field-registry.ts`, `docs/security/*.md`).
3. Archive under `docs/archive/2026-09-lean/` (preserving sub-paths, `git mv`): `docs/comprehensive-beta-implementation-program-2026-08-25.md`, `docs/program-completion-plan-2026-08-28.md`, `docs/program-completion-plan-2026-08-29.md`, `docs/remaining-work.md`, `docs/prd-phase-13.md`, `docs/auth-caching-improvements-2026-07-12.md`, `docs/ba-fresh-db-provisioning.md`, the rest of `docs/product-readiness-program-2026-07/` (22 md + `ai-governance/`), `docs/design/current-state-settings-2026-08-19.md`, `docs/design/current-state-dashboard-and-reviews-2026-08-19.md`, `docs/design/ui-ux-overhaul-proposal-2026-08-19.md`, `docs/research/`, `docs/performance/`, and the twelve dated `docs/operations/*-2026-0{8,9}-*.md` snapshots (`closed-beta-release-runbook-2026-08-19`, `model-switch-5-6-luna-2026-08-19`, `ai-implementation-review-2026-08-19`, `closed-beta-import-defects-2026-08-19`, `ai-latency-and-release-plan-2026-08-19`, `closed-beta-capability-verification-2026-08-21`, `development-database-drift-diagnosis-2026-08-28`, `gate-inventory-2026-08-31`, `capability-state-2026-09-02`, `blocked-capabilities-survey-2026-09-02`, plus the two remaining `*-2026-08-*` files a glob finds). Delete `docs/deep-review.md`. Fold `MIGRATION.md` (42 lines) into `README.md` and delete it. Update links that point at moved files (`git grep -n "comprehensive-beta-implementation-program-2026-08-25\|program-completion-plan-2026-08-2"` — remaining hits after this WP must be inside `docs/archive/`, `docs/release-evidence/`, or files deleted in WP1.4/WP1.5: `src/contexts/badge/CONTEXT.md:122`, `leaderboard/CONTEXT.md:127`, `docs/operations/immutable-release-promotion.md:7`, `railway-data-cells.md:6`, `external-verification-execution-pack.md:5`, `docs/release-evidence/beta/README.md:6`).
   `check:ai-governance-artifacts` (in `lint:ci`) regenerates outputs under the archived `ai-governance/` tree: delete the gate now — script file (grep `check-ai-governance-artifacts`), its `package.json` entry, its `lint:ci` mention, its catalogue row; the generator `scripts/generate-ai-*` files stay only if something else imports them (grep; else delete).
4. Delete the review-ledger apparatus: `scripts/review/` except `zod-v4-conformance.ts` + `.test.ts` (used by `lint`) and except `legal-document-registry.ts`, `legal-document-registry.test.ts`, `legal-draft-consistency.test.ts` (deleted in WP1.5 with the legal stack); move `scripts/review/reconcile-publication-operator.test.ts` → `scripts/ops/reconcile-publication.test.ts` and `scripts/review/report-capability-refusal-operator.test.ts` → `scripts/ops/report-capability-refusal.test.ts` (fix the `../ops/` imports to `./`; `../../src/` depth is unchanged). Delete `package.json:101-105` (`review:freeze-baseline`, `review:validate-program-status`, `review:validate-pre-fix-oracles`, `review:run-pre-fix-oracle`, `review:reachability-proof`) and their catalogue rows (`entry-point-catalogue.ts:5619, 5627, 5636, 5668`).
5. Delete evidence JSON whose only readers were deleted above: `docs/release-evidence/review/{comprehensive-program-status-2026-08-26.json, program-completion-backlog-2026-08-28.json, independent-review-2026-08-29.json, accessibility-debt-register-2026-08-28.json, pre-fix-oracle-index-2026-08-26.json, finding-revalidation-fragments/}`; delete `docs/release-evidence/beta/**` except `beta/README.md`, first moving `docs/release-evidence/beta/local-draft/scale-dataset.json` → `scripts/perf/scale-dataset.json` and repointing `scripts/perf/load-test.ts:423`, `seed-scale.ts:18,54`, `write-scale-evidence.ts:19-21,95`. Keep `docs/release-evidence/review/718fad18…/` and `509cb0a8/` (read by release code deleted in WP1.5) and the 16 markdown files.
6. Delete the prose tests that pin the old authority: `src/shared/governance/documentation-authority.test.ts`, `src/shared/governance/accessibility-debt-register.test.ts`, `src/shared/architecture/current-design-authority.test.ts`.
7. `README.md` and root `CONTEXT.md`: one link to `docs/BETA.md` as the authority; delete `CONTEXT.md:144-161` (`can()` recipe) and fix `:139` (cites superseded ADR 0004) and `:258-260` (absent harness paths).

Verification: local gate; `pnpm test:unit -- scripts src/shared/governance` green; `git grep -ln "docs/release-evidence" -- src scripts` returns only `src/shared/release/candidate-freeze-record.ts`, `scripts/release/*`, `scripts/ops/cutover-single-us-data-cell.ts`, `src/shared/auth/beta-capabilities.ts` (comments) — all WP1.5/Phase 3 targets.

### WP1.2 — Tests to the keep-list; one unit run; no laptop pins (TEST.md rows 1, 2, 11)

Measured baseline at `a9d7ffe1` (node 22.23.2): `pnpm test:unit` 1,307 files, 12,791 passed, 5 skipped, 86–182 s wall across four runs. The audit's 616 s was node-26 drift and its "≈135 s of unit execution" saving does not exist — the whole suite spends 58–114 s in tests, the rest is setup. This WP is justified by maintenance cost and by removing the source-text/catalogue tests that would fail on every WP1.4–1.7 deletion. Record `time pnpm test:unit` on an idle machine before and after the purge.

1. `src/shared/architecture/*.test.ts` — **keep 21**: `tenant-predicate-canary`, `no-auto-publish`, `atomic-family-outbox`, `atomic-inbox-outbox`, `atomic-review-outbox` (there are 3, not 4), `privacy-exfiltration-canary`, `content-free-facts`, `guest-contact-containment`, `retired-review-islands`, `retired-property-access-islands`, `retired-runtime-authorities`, `google-performance-live-boundary`, `provider-target-selection` (the googleapis allowlist WP1.8 trims), `browser-reachability` (ADR 0015), `cross-context-public-api` (runs the ESLint rule), `observability-canary`, `observability-schema`, `portal-atomic-lifecycle`, `simulation-invocation` (until WP2.4), `outbox-consumer-registration` and `delayed-policy-delegation` (delete only their source-text describes at `:57-60` and `:30-33`; keep the container-behaviour cases), plus the two `*.integration.test.ts` files (integration project). **Delete the other 37**: `adr-0030-presence`, `adr-0032-0033-presence`, `adr-0048-presence`, `adr-0052-presence`, `context-acceptance-matrix`, `composition-container-boundary`, `runtime-config-injection`, `legacy-recognition-active-surfaces`, `shared-context-ownership`, `fleet-aggregates`, `ai-egress-deployment`, `authorize-server-boundary`, `error-monitoring-wiring`, `fallow-configuration`, `named-cross-context-seams`, `shared-dependency-policy`, `security-headers-wiring`, `no-event-bus-after-db-write`, `domain-error-convention`, `outbox-import-boundary`, `google-sidecar-artifacts`, `dark-consumer-gating`, `product-state-remount-boundaries`, `legacy-policy-ui-active-surfaces`, `query-loader-payload-ownership`, `portal-group-goal-cache-ownership`, `job-redis-runtime-wiring`, `shared-events-type-only`, `guest-public-response-security`, `google-import-content-lifecycle-ownership`, `tanstack-server-fn-api`, `guest-fact-wiring`, `tenant-cache-transition-ownership`, `auth-lifecycle-composition`, `review-lifecycle-consumer-boundary`, `invitation-rate-limit-wiring`. Delete the data modules that only those tests read: `src/shared/architecture/ambient-runtime-read-authority.ts` and `shared-dependency-policy.ts`.
   Replace `runtime-config-injection`'s one real rule with ESLint: in `eslint.config.js`, for files `src/routes/**` and `src/contexts/**` (the forbidden prefixes from `ambient-runtime-read-authority.ts:233-236`) add `'no-restricted-syntax': ['error', { selector: "MemberExpression[object.object.name='process'][object.property.name='env']", message: 'Read configuration through the container, not process.env' }]`.
2. `src/shared/governance/*.test.ts` — **keep**: `capability-fate.test.ts`, `capability-refusal.test.ts`, `protected-field-registry.test.ts`, `metric-read-authority.test.ts` (last two shrink in WP1.6). **Delete**: `entry-point-catalogue.test.ts`, `event-job-catalogue.test.ts`, `context-standards-matrix.test.ts`, `context-standards-matrix.application.test.ts`, `context-standards-matrix.event-envelope.test.ts`, `context-standards-authority.test.ts`, `context-public-interface-authority.test.ts`, `gate-wiring.test.ts`, `non-fk-reference-surfaces.test.ts`, `counsel-decision-checklist.test.ts`, `legal-approval-authority.test.ts`, `legal-link-targets.test.ts`, `legal-document-registry.test.ts`, `raw-role-decision-catalogue.test.ts`, `data-fate-authority.test.ts`, `contraction-inventory-registry.test.ts`, `operator-command-mutation-classifier.test.ts`, `infrastructure-factory-style-authority.test.ts`, `adr-index.test.ts`.
3. Coverage: delete the `test-unit-coverage` job (`.github/workflows/ci.yml:111-145`) and remove it from `check.needs` (`:415`); delete `scripts/check-coverage.mjs` and `package.json:118` `check:coverage`. Keep the `coverage` block in `vitest.config.ts:56-73` for manual runs.
4. `package.json:34` → `"test": "vitest run --project=unit"`; fix the stale comment at `vitest.config.ts:18-21`.
5. Delete `src/shared/testing/pinned-runtime.test.ts` and `src/shared/testing/pinned-schema-tools.test.ts` (pins script strings). In `scripts/check-test-quality.mjs` delete `PINNED_RUNTIME`/`RUNTIME_DRIFT` (:102-105), the fence filter (:287-311), and every `runtimeFence: true` flag (:143-179); the `.only`/unregistered-skip checks stay.
6. Delete `src/shared/db/retention/retention-registry.test.ts:93-115` (the block reading `docs/legal/*.json`).

Verification: `pnpm test:unit` green with the recorded wall time (expect ≥ 135 s lower); `pnpm test` runs only the unit project (no Postgres needed: run with `DATABASE_URL` unset); `ls src/shared/architecture/*.test.ts | wc -l` = 23 (21 unit + 2 integration).

### WP1.3 — Squash the schema history to one regenerated baseline (DATA.md row 1; corrected mechanics)

Facts: the staged migrator (`src/shared/db/staged-drizzle-migrator.ts`) throws when `0033_medical_morg` is absent (`:135-152`), runs three enum preflights between batches (`:379-382`) and a review-source backfill keyed on `REVIEW_SOURCE_MIGRATION_MILLIS` (`:254-260`) — all specific to replaying history onto a populated database, which no longer exists. The journal table is `drizzle.__drizzle_migrations(id serial, hash text, created_at bigint)`; readers that stay must keep working: `src/shared/health/readiness.ts:57-76` (journal entry count vs row count), `src/shared/db/schema-drift.ts:655-673,1164-1207` (per-entry hash of the on-disk SQL vs DB row), `scripts/migrate-deploy.ts:85-90,205-232` (count parity).

1. Delete the Google-import compatibility surface (its five tables live outside the migratable barrel and would otherwise vanish silently): `src/shared/db/schema/google-import-compatibility.schema.ts`, `src/shared/db/schema/google-import-final-schema.test.ts`, `src/shared/ops/google-import-compatibility-adapter.ts`, `src/shared/ops/google-import-compatibility-lifecycle.ts` (+ tests), the entries at `src/shared/db/contraction-schema-modules.ts:13,32` and `src/shared/db/schema-drift.ts:28,395-398`, `tsup.google-import-compatibility.config.ts`, `Dockerfile.google-import-compatibility`, the `google-import-compatibility` matrix entry at `.github/workflows/ci.yml:447`, the `build:google-import-compatibility` (`ci.yml:364`) and `check:google-import-artifacts` (`ci.yml:378-379`) steps with their `package.json` scripts and script files, `scripts/migrations/google-import-contract.sql`, `scripts/local-stack/google-import-release-drill.ts`, `src/shared/testing/google-import-release-source.ts`, `docs/operations/*google-import*cutover*.md` → archive. Completion grep: `git grep -n "google-import-compatibility\|googleImportCompatibility\|GoogleImportCompatibility\|google_import_compatibility" -- ':!docs/archive' ':!review'` → none.
2. Delete the beta-acceptance and BQC harnesses (TEST.md row 5; both pin pre-cutover tags that a squash removes): `scripts/beta/`, `scripts/bqc/`, `package.json` scripts `beta:*`, `local:beta:*`, `bqc:*`; `.github/workflows/ci.yml` jobs `beta-acceptance-groups` (:1117) and `beta-acceptance` (:1215); the `--mode=beta` scenario files under `src/shared/testing/` that reference `beta-local-1`, `pre-cutover` or `scale-dataset` (`git grep -ln "beta-local-1\|pre-cutover\|create-pre-cutover-dump" -- src scripts e2e` → delete each test file; production modules in the hit list are `scripts/local-stack/stack.ts` (Phase 3 WP3.7) — leave). Delete their catalogue rows.
3. Move the Google property binding index into the schema: in `src/shared/db/schema/property.schema.ts` add `uniqueIndex('properties_org_gbp_location_id_unique').on(t.organizationId, t.gbpLocationId).where(sql\`"gbp_location_id" IS NOT NULL AND "deleted_at" IS NULL\`)`(name from`scripts/google-property-binding-index.ts:3-4`, definition from `:238-240`); delete `scripts/google-property-binding-index.ts`, `package.json:59` `db:google-property-binding-index`, its call at `scripts/migrate-deploy.ts:157-164`, `ci.yml:211`, `.github/workflows/simulation.yml:68`, the step in `src/shared/testing/test-db-setup.ts:283-297`, and its catalogue row. Other importers of `GOOGLE_PROPERTY_BINDING_INDEX*` (grep) move the constant next to the schema or lose the import.
4. Extract the DB-only constructs from a database migrated by the **current** journal (do this before touching `drizzle/`): `pnpm db:reset`-equivalent on a scratch DB (`dropdb`/`createdb`, `DEPLOY_MIGRATE=1 pnpm db:migrate-deploy`), then `psql -At -c "SELECT pg_get_functiondef(p.oid) || ';' FROM pg_proc p WHERE p.pronamespace = 'public'::regnamespace ORDER BY p.proname"` and `psql -At -c "SELECT pg_get_triggerdef(t.oid) || ';' FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid WHERE NOT t.tgisinternal AND c.relnamespace = 'public'::regnamespace ORDER BY c.relname, t.tgname"`, plus any `CREATE EXTENSION` statements found by `git grep -n "CREATE EXTENSION" drizzle/`. Concatenate into `src/shared/db/db-constructs.sql` (functions, then triggers). Drop superseded function versions: `start_google_execution_permit_v1`/`_v2` and any `*_v<N>` with a higher version present and no caller (`git grep -n "<name>(" -- src scripts services src/shared/db/db-constructs.sql`). The permission-version sidecar (`scripts/migrations/2026-07-06-permission-version-triggers.sql`, `bump_permission_version` + `tgr_bump_perm_*`) is included by this extraction; delete `scripts/migrations/` entirely (the 12 orphans, the sidecar, and `0000-auth-tables-bootstrap.sql` — Better Auth's runtime migrator is the only auth DDL path), together with `package.json:63` `db:bootstrap-auth`, `src/shared/db/auth-bootstrap-compatibility.integration.test.ts`, the sidecar reads at `scripts/migrate-deploy.ts:70-74,166-168`, `src/shared/testing/test-db-setup.ts:29-33`, `src/shared/db/schema/db-only-constructs.ts:43-44`, `src/shared/db/disable-guard-triggers.ts:3`, `.github/workflows/ci.yml:212`, `simulation.yml:69`, and the catalogue rows `entry-point-catalogue.ts:5979-6046` and classifier rows `operator-command-mutation-classifier.ts:201-215`.
5. Add `scripts/db-baseline.ts` (no equivalent exists; `scripts/beta/create-pre-cutover-dump.ts` concatenated history, not schema) and `package.json` `"db:baseline": "tsx scripts/db-baseline.ts"`: it `rm -rf drizzle/`, runs `drizzle-kit generate --name baseline`, runs `drizzle-kit generate --custom --name db_constructs`, and copies `src/shared/db/db-constructs.sql` over `drizzle/0001_db_constructs.sql`. Add `scripts/db-reset.ts` with `"db:reset": "tsx scripts/db-reset.ts"`: parse `DATABASE_URL`, refuse non-localhost hosts unless `ALLOW_REMOTE_TEST_DB=1` (reuse `validateTestDatabaseTarget` from `src/shared/testing/test-environment.ts`), connect to the maintenance database (`maintenanceUrl` at `src/shared/testing/test-db-setup.ts:63-67` is the pattern), run `DROP DATABASE IF EXISTS <db> WITH (FORCE)` then `CREATE DATABASE <db>`, then spawn `DEPLOY_MIGRATE=1 pnpm db:migrate-deploy`.
6. Replace `db-only-constructs.ts` (1,863-line hand registry) with a parser: `DB_ONLY_CONSTRUCTS` becomes the list of `{ kind: 'function' | 'trigger', name }` parsed from `src/shared/db/db-constructs.sql` (`CREATE OR REPLACE FUNCTION public.<name>(` and `CREATE TRIGGER <name>`); keep the export name so `schema-drift.ts:29` and `disable-guard-triggers.ts` compile; drop the `owner`/`source` fields and update their readers (grep `DB_ONLY_CONSTRUCTS`).
7. Migrator: `scripts/migrate-drizzle.ts:5,18` and `scripts/migrate-deploy.ts:56,139` call `migrate(drizzle(client), { migrationsFolder: MIGRATIONS_FOLDER })` from `drizzle-orm/node-postgres/migrator` (same journal table, same hash/`created_at` semantics); delete `src/shared/db/staged-drizzle-migrator.ts` and `.test.ts`. `migrate-deploy.ts` keeps: the advisory lock, the Better Auth track, `bindSingleUsDataCellCutoverTarget` (until WP3.2), `sidecarFunctionIsolationSql` (until WP2.3), `initializeReviewProviderSubjectKeyInventoryFromEnvironment`, and the completeness check (`has_sidecar` now means "`bump_permission_version` exists", which the constructs file guarantees).
8. Run `pnpm db:baseline`; commit `drizzle/` (4 files). Delete the migration-text tests: the 33 files that read `drizzle/NNNN_*.sql` (find them with `git grep -ln "drizzle/0[0-9][0-9][0-9]_" -- src scripts e2e`; 28 of them are `src/shared/db/*-migration*.test.ts`, the rest sit under `src/contexts/{ai,inbox,review}/infrastructure/**` and `src/shared/{ai-profile-catalogue-migration,ai-property-calendar-profile,auth/beta-interactive-role-migration}.test.ts`), `src/shared/db/schema-drift-check-registration.test.ts`, and any test failing on a journal count/tag under the failing-test policy (expected: `src/shared/release/candidate-freeze-record.test.ts`, `gate-f-live-evidence.test-fixtures.ts`, `promotion-readback-evidence.test.ts`, `scripts/release/freeze-release-candidate.test.ts` — all WP1.5 targets; delete them now). Keep `src/shared/db/migration-verification.test.ts` (semantic model-vs-database check; must pass against the baseline).
9. Rewrite `src/shared/db/CONTEXT.md:9-56` to the three-file journal and the schema-change loop; `drizzle.config.ts:31-47` comment likewise; `Dockerfile:162-164` unchanged (copies 4 files now).

Verification: `pnpm db:reset` on an empty database succeeds; `pnpm check:schema-drift` reports zero drift; `pnpm test:integration` green; `pnpm test:unit` green; `psql -At -c "select count(*) from pg_proc where pronamespace='public'::regnamespace"` equals the function count in `db-constructs.sql`; `pnpm dev` → `curl -s -o /dev/null -w '%{http_code}' localhost:3000/api/health/ready` → `200`. `ls drizzle drizzle/meta` shows exactly `0000_baseline.sql 0001_db_constructs.sql 0002_db_seed.sql meta/_journal.json meta/0000_snapshot.json meta/0001_snapshot.json meta/0002_snapshot.json`.

**Executed 2026-09-06 — four things the plan did not predict, all now in the tree:**

1. **A DDL-only baseline boots an empty control plane.** 20 of the 182 migrations carried 145 `INSERT` statements. Replaying them is wrong (they were incremental), so the _resulting_ 63 rows were captured from the fully-migrated database into `src/shared/db/db-seed.sql` and generated as `drizzle/0002_db_seed.sql`. Without it 506 integration tests failed on a missing AI/metric/topology control plane.
2. **Generated constraint names differ from the hand-written ones** — 33 foreign keys and 16 primary keys. No runtime code matches on a constraint name (verified by grep over `src scripts services`); three tests and one comment did, and now assert the violation rather than its generated name. The model is the authority, so the derived names are kept.
3. **The old journal left 10 foreign keys `NOT VALID`** — real repair debt that `legacy-people-team-inventory` reported as the `unvalidated_foreign_keys_require_repair` blocker. The regenerated baseline validates all of them; the test now asserts that.
4. **Deleting the compatibility schema left live code naming its five tables.** `postgres-recovery-fence.ts` and `single-us-data-cell-cutover.ts` still counted and UPDATEd them, so the `legacyImportJobsCanceled` / `legacyImportEffectLeasesReleased` fence fields and two cutover blockers were removed end-to-end. The Integration purge no longer defers anything, so its receipt no longer carries the `compatdeferred` marker and `integrationFootprint` lost its hardcoded-zero `legacy` term. `scripts/migrations/google-import-contract.sql` was dropped as dead.

**Also unpredicted:** `drizzle-kit` writes one snapshot per migration (not one total), emits tables → foreign keys → indexes (a composite FK referencing an _indexed_ column fails, so `db-baseline.ts` hoists unique indexes above foreign keys), spells `DESC` as `DESC NULLS LAST` (7 indexes; 6 on `NOT NULL` columns, the 2 nullable ones back no `ORDER BY`), and cannot express the 7 `btree_gist` exclusion constraints — those are appended to `db-constructs.sql` by hand.

### WP1.4 — Delete `team`, `badge`, `leaderboard` outright (ARCH.md row 1, corrected blast radius)

Facts: all three `build.ts` are inert (`team/build.ts:9-16`); the only production composition importer is `src/composition/organization-export-contributors.ts` (`:23,29,36,40,45,52`); `src/composition.ts` and `src/bootstrap.ts` contain no reference. 17 tables; the only inbound FK from outside the set is `staff_assignments.team_id → teams.id`, and `staff_assignments` is in the set.

1. Delete `src/contexts/team/`, `src/contexts/badge/`, `src/contexts/leaderboard/` (65 files).
2. `src/composition/organization-export-contributors.ts`: remove the six imports and array entries; `NON_IDENTITY_EXPORT_CONTRIBUTOR_COUNT` (`:56`) 16 → 13; fix the "seventeen contexts" comment (`:119`) and `buildOrganizationLifecycleContributors` (`:130+`).
3. `src/shared/events/events.ts:32-37,138,148,151,165`: drop `TeamEvent`/`BadgeEvent` from the master union; `src/shared/events/schema-registrations.ts:851-872,1647-1662`: delete the `team.created/updated/deleted` schemas and registrations.
4. Delete `src/shared/testing/in-memory-team-repo.ts`; remove the `Team` import and fixtures at `src/shared/testing/fixtures.ts:17,110-131`; `src/shared/CONTEXT.md:202`.
5. Delete `scripts/ops/reconcile-people-team.ts`, `scripts/ops/report-legacy-people-team.ts`, `scripts/ops/report-legacy-recognition.ts` and their `package.json` `ops:*` entries; remove `verifyPeopleCutoverPromotionReadiness` and its call site from `scripts/release/deploy-beta.ts:108` (file dies in WP1.5) and the `'src/contexts/team'` entries in `scripts/release/release-authority-digest.ts:18` + `.test.ts:21,49`.
6. Notification `badge.awarded`: delete the type at `src/contexts/notification/domain/types.ts:59`, the category map row `notification-delivery-policy.ts:63`, `renderBadgeAwarded` (`notification-templates.ts:345-388`) and its matrix row (`:391`), `badgeName?` (`notification-payload.ts:56-57`), `BETA_DARK_NOTIFICATION_TYPES` (`beta-notification-trigger-matrix.ts:212-214`, and its consumers — grep), the icon row `src/components/features/notification/notification-utils.ts:146`, the story fixture `notification.stories.fixtures.ts:138-141`, and the assertions in `beta-notification-trigger-matrix.test.ts:18`, `notification-delivery-policy.test.ts:108`, `notification-templates.test.ts:159-163`, `types.test.ts:119`, `outbox-notification-delivery.test.ts:189`; `src/contexts/notification/CONTEXT.md:210-211`.
7. Governance mentions: `entry-point-catalogue.ts` (rows at `:62,69,76,179-182,509,516,523,3429-3435,3542-3547,3599,5010-5015,5033,5109-5114,5118-5123,6039`), `event-job-catalogue.ts` (`TEAM_ROWS` `:1365-1418`, `BADGE_ROWS` `:2394+`, `DARK_CONTEXT_MODULE_RE` `:182`, `:356,366,2290`), `data-fate-authority.ts` (`:239-247,495-507,650-654,834-839`), `capability-fate.ts:107-122` (delete `team.use`, `badge.use`, `leaderboard.use` rows) and the same three keys in `src/shared/auth/beta-capabilities.ts` (`CAPABILITIES` `:55,57,58`, `:145`, `BLOCKED_CAPABILITIES` `:162-164`, `DARK_CONTEXT_CAPABILITIES` `:642-648`), `capability-fate.test.ts:58-72` (`legacy_blocked` now empty — delete that assertion; the three `gbp.*` `permanently_denied` assertion stays), `context-public-interface-authority.ts:301-306`, `context-standards-matrix.ts:87-88,96,107,137,142,149,156,284-286`, `non-fk-reference-surfaces.ts:14-22,74-78,85-110,245-248`, `contraction-inventory-registry.ts:79-125`, `src/shared/db/schema/migratable.ts:17,35,53` (+ the `staff-assignment.schema` export line), `.fallowrc.json:177-178,250-251,254-255,329-330,394-428,484-494`.
8. Schema: delete `team.schema.ts`, `badge.schema.ts`, `leaderboard.schema.ts`, `staff-assignment.schema.ts`; in `people-access.schema.ts` delete the `teams` import (`:26`), `teamMemberships` (`:219-268`) and `teamPortalGroupScopes` (`:325-366`); delete the seven `owner: 'recognition'` constructs (`reject_recognition_immutable_mutation` + six `*_append_only` triggers) and `staff_assignments_perm_ver_iud` from `src/shared/db/db-constructs.sql`; remove `src/shared/db/schema/recognition-governance-constraints.test.ts`; `pnpm db:baseline && pnpm db:reset`.
9. The five tests that SHA256-pin `capability-fate.ts` bytes: three die with the trio; delete the digest assertions in `src/contexts/guest/infrastructure/adapters/guest-organization-lifecycle.adapter.test.ts:266-270` and `src/contexts/portal/infrastructure/adapters/portal-organization-lifecycle.adapter.test.ts:261-265`.
10. Routes: delete `src/routes/_authenticated/leaderboard.tsx`, `src/routes/_authenticated/settings/recognition.tsx`, `-leaderboard.test.ts`, `settings/-recognition.test.ts`; delete the `/leaderboard` and `/settings/recognition` cases at `e2e/critical/beta-product-journeys.spec.ts:387-393,1034-1051`; regenerate `src/routeTree.gen.ts` (tracked) via `pnpm build` or `pnpm dev`.
11. ADRs: delete `docs/adr/0013-*.md`, `0014-*.md`, `0021-*.md`, `0043-*.md`; remove their rows from `docs/adr/README.md`; fix links (`git grep -n "adr/00\(13\|14\|21\|43\)"`). `PRODUCT.md`/`DESIGN.md`: delete sections describing badges, leaderboards, teams or staff mobile progress as current product.
12. Completion grep: `git grep -n "contexts/team\|contexts/badge\|contexts/leaderboard\|badge\.awarded\|team_memberships\|team_portal_group_scopes\|staff_assignments\|recognition_\|leaderboard_" -- src scripts e2e .storybook services` → none.

Verification: local gate + `pnpm test:integration`; `pnpm check:schema-drift` clean; `psql -At -c "select count(*) from information_schema.tables where table_schema='public'"` is 17 lower than after WP1.3.

**Executed 2026-09-06 — satisfied: 216 tables (233 − 17), zero drift, typecheck 0 errors, lint:ci green, unit 1,152 files / 10,985 passed, integration 212 files / 1,156 passed.** Four scope corrections the plan did not predict:

1. **The people-authority reconciliation had to come forward from WP3.4.** Its legacy query reads `staff_assignments` _and_ `teams` in one statement, so deleting `team` makes it unbuildable — the phase assignment was wrong, not the deletion. Deleted with it: the whole legacy staff-assignment path in `src/contexts/staff/` (13 files + `staff-command-store.ts`), the four `people-authority-reconciliation.*-query.ts` SQL bodies (orphaned once the repository went), `scripts/ops/{reconcile-staff-grants,report-people-authority}.ts`, and `src/shared/domain/ids.ts`'s `StaffAssignmentId`. `build.ts` had already documented the path as "source-only for reconciliation" and never wired it; `staff_assignments` held zero rows. The live People page uses the governed participation model and is untouched.
2. **A live deploy sidecar created a trigger on a dropped table.** `scripts/migrations/2026-07-06-permission-version-triggers.sql` — which every `db:migrate-deploy` applies — declared `staff_assignments_perm_ver_iud`. That would have failed every deploy. Also deleted three `scripts/migrations/*.sql` one-offs whose SQL creates or indexes dropped tables.
3. **`git rm -r` leaves empty directory trees**, and `boundedContextDirectories()` reads directories, so `property-erase-contributor.port.test.ts` still saw a `team` context after every file was gone. `rm -rf` the three directories.
4. **The deletion emptied three vocabularies, not just three lists.** `legacy_blocked` lost every `CapabilityFate` member, `StaffEvent` lost every member (the context emits nothing — `staff.assigned`/`staff.unassigned` were the only two), `CONTRACTED_RUNTIME_CONTEXTS` lost every entry, and `NON_FK_REFERENCE_SURFACES` lost its only `'declared'`-scope rows. Each was deleted rather than left as an inhabitable-by-nothing type. Two tests were retargeted rather than re-pinned: the non-FK scanner integration test now uses `goals` (the surviving activity referent) and its read-only-fence INSERT was made otherwise-valid so it cannot pass for the wrong reason. One test was deleted outright — `organization-export-contract.test.ts`'s byte-identity guard existed to prove one past type-only extraction was neutral; re-pinning its digest to a 14-contributor bundle would assert nothing. Its other six contract cases stay.

Also fixed in passing: `env.ts`'s `BETA_E2E_GLOBAL_CAPABILITIES` example listed `identity.register,organization.create,team.use` directly under "Must never enable blocked capabilities" — all three are blocked. It now names `goal.use,portal.write,notification.send_email`, which are `controlled_beta`.

### WP1.5 — Delete the release ceremony, IaC and legal registry (CI.md rows 3–4, ADR.md rows 2–4, SEC.md rows 1, 3, 4, 6)

Nothing here is invoked by a workflow that has ever run (`release-images.yml`: 0 runs). Corrected facts: `src/shared/release/gate-policy.ts` has three production importers only for its re-export of `CURRENT_RELEASE_POSTURE` (`:32`); `gate-f-evidence.ts:29` imports `legal-approval-checklist.ts`, so the legal stack and Gate F must go together.

1. Delete `.github/workflows/release-images.yml`, `.railway/` (1,952 lines incl. tests), the six sidecar `railway.*.json` files (keep `railway.json` and `railway.worker.json`), `scripts/release/` entirely (41 files), `security/gate-f-approval-roles.json`, `security/rel-01-canary-threshold-profile.json`, `docs/legal/{counsel-decision-checklist.json, legal-document-registry.json, implementation-facts-2026-08-26.md, revision-set.schema.md}`, `docs/governance/standards-exceptions.json`; `package.json` scripts `release:*` (14) and `infra:*` (6).
2. Delete `src/shared/release/` except the modules that survive by import from outside the release tree: run `git grep -n "shared/release/" -- src scripts services server ':!src/shared/release'` and keep exactly the files it names (expected: `release-posture.ts`, `json-shape-guards.ts`, the digest-map/promotion-manifest parser used by `scripts/ops/deploy-ci-images.ts`); repoint `live-evidence` importers are gone with the directory. Delete all `src/shared/release/*.test.ts` and `*.test-fixtures.ts` except tests of the surviving modules.
3. Delete the legal stack: `src/shared/governance/{legal-document-registry,legal-approval-authority,legal-link-targets,counsel-decision-checklist}.ts`, `scripts/review/{legal-document-registry.ts,legal-document-registry.test.ts,legal-draft-consistency.test.ts}`, `check:legal-registry` from `lint:ci` and its script entry, `src/shared/observability/beta-support-operations.test.ts:3-4` imports (fix the test or delete the cases), `src/shared/db/retention/retention-registry.ts:14,32,147` prose. Add `status: draft` frontmatter to `docs/legal/privacy-notice.md`, `internal-beta-agreement.md`, `google-access-disclosure.md`; create `docs/legal/CHANGELOG.md` with one dated line. Keep the DSAR export path (`identity-organization-export-contributor.ts`).
4. `src/shared/governance/gate-policy.ts`: delete; change the three importers' `CURRENT_RELEASE_POSTURE` import to `#/shared/release/release-posture` if they survive step 2 (they are `live-evidence/*` and do not).
5. `scripts/ops/deploy-ci-images.ts`: keep; if it imports deleted modules, remove those imports and the seven-image validation with them (CI.md row 11 shrinks it in WP2.5).
6. Archive `docs/operations/{immutable-release-promotion.md, railway-data-cells.md, closed-beta-ci-image-delivery.md, external-verification-execution-pack.md, google-content-approval-closed-beta.md}` → `docs/archive/2026-09-lean/operations/`; delete `docs/release-evidence/review/718fad18…/` and `509cb0a8/` (their readers are gone). Delete `docs/adr/0058-*.md`, `0059-*.md`, `0060-*.md` (RETIRE) and fix `docs/adr/README.md` and links.
7. `railway` SDK devDependency: delete if `git grep -ln "from 'railway'\|from \"railway\"" -- src scripts` is empty.

Verification: local gate; `git grep -n "gate-f\|live-evidence\|legal-document-registry\|create-promotion-manifest\|deploy-beta" -- src scripts services .github package.json` → none; `pnpm lint:ci` green.

**Executed 2026-09-06.** Corrections to the plan's figures and scope:

1. **Eight root `railway.*.json` sidecar configs, not six** — the plan missed `railway.perf-runner.json` and `railway.sandbox.json`. `railway.json` and `railway.worker.json` survive.
2. **`scripts/release/` held 39 files, not 41** — WP1.4 had already removed `deploy-beta.ts` and its test.
3. **`src/shared/release/` collapsed 70 → 9 files.** Step 2 says to keep the modules imported from outside the release tree, but almost every such import came from `scripts/release/`, which step 1 deletes. Computing the fixpoint instead of a one-shot list leaves exactly `release-posture`, `json-shape-guards`, `google-approval-gap`, `data-cell-cutover-evidence`, `railway-deployment-profile` and their tests. `railway-project-service-isolation` looked alive only because `package.json`'s `infra:railway:validate` named its test file. Five test/fixture files were left with no subject and went too.
4. **Two live CI gates read deleted files** and would have thrown on the next run: `check-container-image-policy.ts` `readFileSync`s `.github/workflows/release-images.yml` in three places, and `check-runtime-environment-contract.ts` hashes `.railway/railway.ts` as one of its 11 contract files (now 10, snapshot regenerated). The image-policy gate lost the three checks whose only evidence was the deleted workflow and kept everything that reads `ci.yml`, a Dockerfile or `security/container-images.json`.
5. **Two pre-existing `ci.yml` bugs surfaced.** The SBOM step demanded ten image SBOMs while the policy source declares nine. Worse, the `images: >-` folded blocks for the `worker-tools` and `sidecars` matrix groups each repeated their JSON payload verbatim — and a folded block joins those lines, so the value was two concatenated arrays that would not parse as JSON at all. Damage from the WP1.4 matrix repair; both fixed and all three payloads now parse.
6. **The AI operation-profile digests are triplicated and the deletion moved one.** `AI_GATEWAY_BUILD_ATTESTATION_V1` carried a `railwayConfig` field per artifact naming three now-deleted files. Removing them changed `AI_GATEWAY_BUILD_ATTESTATION_DIGEST`, which feeds `artifactAttestations` → `profileDigest` for all four operation profiles — and those digests are duplicated into `db-seed.sql` rows **and** into a literal JSON assertion inside a `db-constructs.sql` readiness function. Seventeen digest occurrences had to be rewritten across both SQL files by hand. **This triplication is a real drift surface Phase 3 should remove** by generating the AI catalogue seed and the readiness assertion from the TypeScript constants instead of hand-maintaining three copies.

**Deliberately deferred:** step 3 deletes `legal-link-targets.ts`, which was the only code recording the unpublishable `/privacy` link. It was deleted — it asserted the gap in a unit test but never blocked anything at runtime — and the record moved into `docs/BETA.md` §2 with the exact payload lines, the five places `MERCHANT_AI_NOTICE_VERSION`/`_DIGEST` are pinned, and the fact that changing the notice requires re-collecting consent. **The privacy-link fix itself is still owed and is a user decision, not an engineering one:** publish the notice at a real `/privacy` route, or drop the two links. A first attempt at the second option was reverted on discovering the notice version is enforced by database CHECK constraints, a database function and three seed rows — it is a schema change, not a text edit.

Satisfied: typecheck 0 errors, `lint:ci` green, prettier clean, zero schema drift, `test:unit` 1,102 files / 10,239 passed, `test:integration` 212 files / 1,156 passed.

### WP1.6 — Governance code with no product reader; promote `capability-fate.ts` (ADR.md rows 5, 7, 12; TEST.md row 1)

1. Delete `src/shared/governance/{context-standards-matrix,context-standards-authority,context-standards-current-tree,context-public-interface-authority,infrastructure-factory-style-authority}.ts` (the comment at `src/contexts/identity/infrastructure/repositories/capability-refusal.repository.ts:34` citing the factory-style file: delete the comment). Trim `docs/standards.md` §4 (CONTEXT.md heading standard and exception machinery; keep §1–3) to ≈150 lines.
2. `operator-command-mutation-classifier.ts` is consumed by `entry-point-catalogue.ts:35` (`classifyOperatorCommandMutation` inside the `ops(...)` row helper — exact usage unverified, confirm first): replace the computed field with a literal `mutation: 'read' | 'write'` on each `ops` row or drop the field if `system-execution-policy.ts`/`delayed-execution-gate.ts` never read it (grep the field name); then delete the classifier.
3. Promote the fate table: in `src/shared/auth/beta-capabilities.ts` replace the hand lists `CORE_CAPABILITIES` (`:85-98`, corrected) and `BLOCKED_CAPABILITIES` (`:151-171`, corrected) with `new Set(listCapabilitiesByFate('core'))` and `new Set(['beta_disabled','safety_blocked','legacy_blocked','permanently_denied'].flatMap(listCapabilitiesByFate))` (mapping proven by `capability-fate.test.ts:42-57`; both lists already agree exactly — verified 2026-09-05 by executing the code: 37 capabilities, `core` 12, `controlled_beta` 12, `beta_disabled` 6, `safety_blocked` 2, `legacy_blocked` 2, `permanently_denied` 3, runtime blocked 13). `capability-fate.ts` must not import `beta-capabilities.ts` (cycle): `CAPABILITY_FATE` is keyed by `Capability`, so move the `CAPABILITIES` tuple + `Capability` type into `capability-fate.ts` (or a new `src/shared/governance/capabilities.ts` if `capability-fate.ts` would otherwise import from auth) and re-export from `beta-capabilities.ts`. **Ordering constraint:** moving the tuple changes `capability-fate.ts` bytes and breaks all five SHA-256 digest pins on it (`{badge,leaderboard,team,guest,portal}-organization-lifecycle.adapter.test.ts`), so WP1.4 step 9 must land first.
4. Shrink `protected-field-registry.ts` (986 lines, test-only) to the Google-content allowlist its test needs (target ≈100 lines: the field classification table only; delete helper layers with no test reader) and `metric-read-authority.ts` (155) to the reader allowlist (≈30 lines); trim both tests accordingly.
5. `lint:ci` (`package.json:30`): remove `check:google-provider-fixtures` (keep the generator script; delete the check script + entry) and `check:runtime-environment-contract` (move its one real tripwire — the `2ed0e536` catch — into a unit test next to `src/shared/config/env.ts`; delete the script). `lint:ci` = `pnpm lint` + `node scripts/check-test-quality.mjs`.
6. Delete `docs/architecture/beta-capability-fate-authority.md` (points at code).
7. Trim `src/shared/domain/source-content-policy.ts`: delete `mayCombineAcrossProperties`, `requiresMerchantOptIn`, `requiresPiiRedaction`, `requiresApprovedProvider` and `requiresHumanReplyPublish` (`:61-66`) plus the header comment at `:4-6` claiming "use cases check this policy … before analyzing, aggregating, caching, or publishing review content". Nothing in `src/` reads any of the five; only `rawContentTtlMs` and `policyVersion` are consumed (`src/composition/operational-readout.ts:148`). Every obligation is enforced elsewhere and `docs/BETA.md` §2 now records where, so the booleans are prose wearing a type. Keep the TTL and the version.

Verification: local gate; `pnpm test:unit -- src/shared/governance src/shared/auth` green; `git grep -n "context-standards\|context-public-interface-authority\|counsel-decision\|operator-command-mutation-classifier\|infrastructure-factory-style" -- src scripts docs ':!docs/archive'` → none; `listCoreCapabilities().length === 12` and `listBlockedCapabilities().length === 10` (13 minus the three trio keys) — assert once in a throwaway `tsx -e` and discard.

**Executed 2026-09-06.** Verification satisfied exactly: `listCoreCapabilities().length === 12`, `listBlockedCapabilities().length === 10`, completion grep clean, typecheck 0 errors, `lint:ci` green, `test:unit` 1,102 files / 10,231 passed, `test:integration` 212 files / 1,156 passed. Four scope corrections:

1. **Step 2 understated the cut.** `EntryPointMutation` has no reader outside `entry-point-catalogue.ts`, and the catalogue's own test died in WP1.2 — so the whole apparatus was code computing a value and then validating the value it had just computed. Rather than replace the derived field with a literal, the two exported types, the four-rule derivation chain (`kindScopedMutation`, `contextCommandMutation`, `requestSurfaceMutation`, `mutationForEntry`), the `entryMutationIssues` validator, 25 classification data tables and 23 per-row overrides were all deleted with the classifier. The catalogue went 6,293 → 5,064 lines.
2. **Step 5's `check:google-provider-fixtures` entry bundles two unrelated checks.** `generate-google-provider-fixtures.ts --check` is the repo-agrees-with-itself class the plan removes, but `check-google-provider-identifiers.mjs` is a leak guard that rejects real Google identifiers appearing in test files, and three test files carry comments relying on it. Only the self-check was dropped; the leak guard stays in `lint:ci`.
3. **Step 5's deletion of `check:runtime-environment-contract` was declined.** That gate exists because commit `739ccbc9` split every sidecar port, kept the repository self-consistent, passed every gate, and crash-looped two live services — a class no repository test can catch, since tests only compare the repo with itself. An attempt to remove its one real cost (it fires on comment-only edits) by hashing comment-stripped source was reverted: `env.ts` contains regex literals, so regex comment-stripping silently swallowed real code and the gate stopped detecting an actual contract change, which a negative control caught. Byte hashing plus a one-command snapshot refresh is the honest trade; the gate stays.
4. **`docs/standards.md` is 344 lines, not ≈150.** §1–3 alone are 230 lines and were kept byte-for-byte; the estimate was low. Removed: §4.1–4.3 (the CONTEXT.md heading standard), the accepted-exception register machinery, and §7.1 and §8.3, whose only named enforcement authority was deleted in step 1.

Also corrected: the plan's fate-group figures. After WP1.4 removed the trio the real partition is 34 capabilities — `core` 12, `controlled_beta` 12, `beta_disabled` 5, `safety_blocked` 2, `permanently_denied` 3 — so blocked is 10. `team.use` was `beta_disabled`, which is why that group is 5 rather than the 6 a naive subtraction gives.

### WP1.7 — Frontend dark surfaces (UI.md rows 2, 3, 6, 9, 10, 13; bugs 2–4; corrected sizes)

Correction: `src/components/features/staff/` is half load-bearing — `staff-participation-form.tsx`, `staff-participation-list.tsx`, `staff-participation-row.tsx`, `portal-responsibilities-modal.tsx`, `portal-selector.tsx`, `types.ts`, `index.ts` and two stories are imported by the live People route (`src/components/features/property/people/staff-tab.tsx:4-9,27`, `people-page.tsx:12,19`) and stay. The `Role` union keeps `'Staff'` (`src/shared/domain/roles.ts:9`): `src/shared/auth/tenant-resolver.ts:307-310` uses it as the lowest-privilege placeholder for custom-role members; removing it is Phase 3 people-merge work.

1. Staff shell — delete `src/routes/_authenticated/home.tsx`; `src/components/features/staff/{staff-home.stories.tsx, use-staff-home-data.ts, use-staff-home-data.test.ts, staff-recent-activity.tsx, staff-portal-filter.tsx, staff-home-kpis.tsx, staff-empty-state.tsx}` (and their `index.ts` exports); `src/components/layout/{staff-sidebar.tsx, staff-sidebar.stories.tsx, staff-property-switcher.tsx, staff-nav-items.tsx}` (`nav-items-shared.tsx` stays; fix its header comment). `src/routes/_authenticated.tsx`: remove the `StaffSidebar` import (`:25`); at `:112` replace `role: org.role ? (org.role as Role) : 'Staff'` with a guard before the assignment — `if (!org.role) throw redirect({ to: '/unavailable', search: { feature: 'Workspace' } })` (same shape as `:107-109`; `category` stays undefined — the enum at `src/routes/unavailable.tsx:12-14` has no role value and none is added) — then `role: org.role as Role`; at `:195-199` render `<ManagerSidebar …/>` unconditionally. Add one case to `src/routes/-_authenticated.test.ts`: organization present, `role: null` → redirect to `/unavailable`. `src/routes/_authenticated/progress.tsx:15` `/home` → `/dashboard`; `src/routes/_authenticated/dashboard.tsx:69` `/home` → `/properties`. Server: delete `src/contexts/dashboard/server/staff-dashboard.ts` (`getStaffDashboardDataFn`), `src/contexts/review/server/staff-recent-activity.ts` + `src/contexts/review/application/use-cases/get-staff-recent-activity.ts` (+test) + its wiring at `src/contexts/review/build.ts:93,190,261,550,628`, `src/contexts/staff/server/staff-portals.ts`; keep `src/contexts/staff/application/use-cases/list-staff-portals.ts` only if `git grep -n "listStaffPortals" -- src` shows a non-deleted caller, else delete it and its `staff/build.ts:118` wiring. Catalogue rows `:2273, :2493, :2775, :3417-3421`. Fix `src/contexts/goal/application/goal-authority-inventory.test.ts:160` (`/home` → `/dashboard`) and delete its stale `:164-165` lines; fix the comment at `src/components/features/dashboard/fleet-overview.stories.tsx:250-251`; `src/contexts/dashboard/CONTEXT.md:125`, `src/contexts/staff/CONTEXT.md` mentions.
2. `/register` — delete `src/routes/register.tsx` (keep `register-form.tsx`, used by `join.tsx:66`); if `getRegistrationGate`/`registerUserAndOrg` in `src/contexts/identity/server/organizations.ts` have no remaining importer (`git grep`), delete the two server functions (keep their use cases — invitation flows may share them; verify by grep). Catalogue row `:3351-3355`.
3. `/` — `src/routes/index.tsx`: replace with `createFileRoute('/')({ beforeLoad: () => { throw redirect({ to: '/login' }) } })`; delete the marketing markup. Catalogue row `:3342-3345`.
4. Closure Center — delete `src/components/features/closure/` (7 files), `src/routes/_authenticated/settings/closure.tsx`, `settings/-closure.test.ts`, the sidebar entry `src/components/layout/settings-sidebar.tsx:100-107`; delete `src/contexts/identity/server/organization-closure-fns.ts` (its seven server fns had one caller; the use cases stay for `ops:report-organization-lifecycle` and the lifecycle scripts). Catalogue rows `:3483-3487` and the seven fns.
5. Hero-image pipeline (`portal.upload` is `safety_blocked`, `capability-fate.ts:85-89`) — delete `src/components/features/portal/portal-form/hero-image-section.tsx`; remove `requestUploadUrl`/`finalizeUpload`/`putFilePresigned` props through `edit-portal-form.tsx:5-6,41-51,59-60,120-131`, `portal-settings/portal-settings.tsx:50-60,88-89,162-163`, `portal-detail/portal-detail-tab-panel.tsx:65-66,84-85,107-108`, `portal-detail/portal-detail-types.ts:67-76`, `src/routes/_authenticated/properties/$propertyId/portals/-portal-detail-actions.ts:8,10,152-153,163-164`, `$portalId.tsx:190-191`, and the story fakes in `portal-detail-page.stories.tsx:127-137,229-230`, `edit-portal-form.stories.tsx:64-74,91-92`, `portal-settings.stories.tsx:44-54,91-92`. Server: delete `requestUploadUrl` (`src/contexts/portal/server/portals.ts:566`), `finalizeUpload` (`:610`), `application/use-cases/request-upload-url.ts`, `finalize-upload.ts` (+tests), wiring at `portal/build.ts:54-55,409,417`, the handler cases `portal/server/portals-handler.test.ts:33-34,72-73,102-103,286-305`, `src/contexts/portal/infrastructure/jobs/process-image.job.ts` and its registration (grep `process-image`), `portal/CONTEXT.md:217`; delete `ImageUploadField`/`putFilePresigned` helpers if importer-less; remove `sharp` from `package.json` and the object-storage config that only it used (`git grep -n "S3_\|MINIO\|putFilePresigned\|process-image" -- src scripts .env.example` → delete env keys whose only reader is gone).
6. 2FA card — delete `src/components/features/identity/security-settings-form.tsx:109-120`. Not cosmetic: the card reads "Two-factor authentication … Coming soon" and no TOTP exists anywhere in `src/`, so the product advertises a feature it does not have. `docs/BETA.md` §1 records that it is deleted rather than implemented.
7. ~~Fleet pagination for six rows (UI row 9)~~ — **dropped 2026-09-05.** The audit read the infinite query as ceremony for six rows; the code says it is a bug fix. `src/routes/_authenticated/dashboard.tsx:38-44` records that this "used to be a plain query that never passed [a cursor], so a fleet of more than fifty properties silently ended at fifty with nothing on screen to say so", and the projection pages at `FLEET_PAGE_SIZE = 50`. `docs/BETA.md` §1 no longer fixes the Property count, so reverting to a plain query re-introduces silent truncation at 51 Properties. Keeping it costs `fleet-load-more.tsx` (40 lines), one stories file and ~15 lines of query config — cheaper than the bug.
8. Component cap: `src/components/CONTEXT.md:31` 150 → 300 and `eslint.config.js:1673-1701` `max-lines` → 300.
9. Regenerate `src/routeTree.gen.ts`; delete `.fallowrc.json` entries for deleted files.

Verification: local gate + `pnpm build && pnpm check:bundles` + `pnpm test:storybook`. Browser (`pnpm dev`): `/` → `/login`; signed-in manager: `/home` → not found; `/settings` sidebar has no Closure Center; portal edit shows no hero upload; security settings show no 2FA card; `/leaderboard` → not found.

**Executed 2026-09-06 — every browser check verified against a running app** (seeded manager, Chrome for Testing): `/` → `/login`; authenticated `/home`, `/leaderboard`, `/register`, `/settings/recognition`, `/settings/closure` all render not-found; the settings sidebar lists Profile/Security/Preferences/Organization/Members/Notifications/AI & replies/Integrations and no Closure Center; security settings contain no two-factor text; the portal edit surface has no hero copy and no file input. Gate: typecheck 0, `lint:ci` OK, `test:unit` 1,094 files / 10,134 passed, `test:integration` 212 / 1,156, `check:bundles` OK (662,464 B of 689,152 B initial closure), storybook 93 files / 540 passed.

Five things the plan did not predict:

1. **CI was red on `main` for every Phase 1 commit, and the cause was mine.** WP1.5 removed the `railway` devDependency from `package.json` without regenerating `pnpm-lock.yaml`, so every job died at `pnpm install --frozen-lockfile` with `ERR_PNPM_OUTDATED_LOCKFILE`. Lockfile regenerated. **Any dependency edit must regenerate the lockfile in the same commit.**
2. **WP1.3 left two CI jobs invoking deleted scripts.** `beta-acceptance-groups` and `beta-acceptance` (154 lines) still ran `pnpm beta:smoke` after `scripts/beta/` was deleted. Both jobs and the orphaned `beta:smoke` script are gone; the workflow is 12 jobs with no dangling `needs`.
3. **The a11y gate had not run at all since the Storybook 10 upgrade.** `@storybook/test-runner` is a Storybook 8/9 tool; under Storybook 10 its jest runtime cannot load the config (`throwHooksUnsupported`), so all 93 suites failed at setup and 0 tests ran — the `storybook-test` job was failing for that reason alone. The runner is deleted and the job now runs the Vitest browser project.
4. **Enforcing a11y on the surviving runner needed a flag split.** `addon-a11y` only throws when it reads `import.meta.env.VITEST_STORYBOOK === "false"`, and `addon-vitest` forwards `process.env.VITEST_STORYBOOK` into it — but this repo gated the project's _existence_ on that same variable being `"true"`, which is exactly why a11y was report-only and the broken runner was kept. Inclusion now keys on `REPKEY_STORYBOOK_TESTS`, and the project is named explicitly in `vitest.config.ts` because the plugin only force-names it when `VITEST_STORYBOOK` is set.
5. **Turning a11y on surfaced 12 violations, all one rule.** Every failure was `aria-hidden-focus` across 6 story files: Radix marks the page behind an open dialog `aria-hidden` while trapping focus inside it, and axe reads that statically. Disabled that one rule with the reason recorded in `.storybook/preview.tsx`, keeping every other axe rule enforced. **Known gap:** console errors no longer fail a story — the deleted runner did that via an allowlist in `.storybook/test-runner.ts`, and the Vitest path filters console output reporter-side only.

Cascade beyond the plan's list, all forced by the deletions: the `/home` loaders (`dashboard/server/staff-dashboard.ts`, `review/server/staff-recent-activity.ts`); the orphaned `listStaffPortals` use case and with it the whole `StaffPortalLookupPort` seam (port, contract test, `StaffContextDeps` member, the composition late-binding closure) — Portal's `getPortalInfo` and `listPortalIdsByProperty` stay, they have live Goal and Guest consumers; the `registerUserAndOrg` use case and its org-signup plumbing (`signUp`, `deleteUser`, `setActiveOrganization`), after confirming no erasure, closure-lifecycle, privacy or ops path reaches `deleteUser` and that `ops:privacy-request` refuses to fulfil at all; and a purge-pending notification that told merchants to open the deleted Closure Center.

**Deferred deliberately:** the hero-image worker path is now unreachable — nothing can emit `portal.hero_image.processing_requested`, and the disposable database has no rows for the cleanup job — but removing it reaches into the S3 dependency (storage port, issued-object adapter, bucket configuration, `compose.local.yml`, the object-lifecycle section of `docs/operations/backup-and-lifecycle.md`). Left in place and compiling: `process-image.job`, `cleanup-upload-sources.job`, `portal-upload-issuance-store.port`, the issued-object half of `storage.port`, `domain/upload-issuance.ts` and their registrations.

### WP1.8 — Pin the invariants the collapse must keep (SEC.md § Post-collapse invariants, corrected)

Only the four pins that are true today are written now; "provider client accepts only the branded canonical source" and "worker boot refuses without the spend-cap config" are WP2.3 entry criteria because the in-process client does not exist yet. The direct-fetch fallback is deleted in WP2.1, when the in-process executor becomes unconditional.

1. New `src/shared/architecture/provider-client-singleton.test.ts`, modelled on `google-performance-live-boundary.test.ts` (`node:fs` walk + comment stripper, no tsc): over non-test files in `src/`, `services/`, `scripts/`, assert (a) exactly one file matches `from ['"]openai['"]` and it is `services/ai-egress-gateway/openai-connector.ts` (constant updated in WP2.3); (b) `openai/helpers/zod` imports occur only under `services/ai-egress-gateway/`; (c) `src/shared/config/env.ts` contains no `OPENAI` token.
2. Trim `src/shared/architecture/provider-target-selection.test.ts` to its allowlist describe (`:73-77`): production files allowed to contain `googleapis.com` (excluding `fonts.googleapis.com`) = `src/shared/google-provider-control/route-catalogue.ts`, `egress-inventory.ts`, `src/shared/auth/pubsub-jwt.verifier.ts`, `src/shared/security/security-headers.ts`, `src/composition/provider-runtime.ts` (`:60-67`), `src/contexts/integration/application/google-provider-contract.ts` (`:11`, an OAuth scope), `src/shared/testing/fixtures.ts`, `src/shared/testing/in-memory-google-oauth-port.ts`; and `fetch(` in `src/contexts/integration/**` allowed only in `gbp-provider-fetch.ts`, `google-oauth.adapter.ts`, `google-review-api.adapter.ts` (a ratchet; WP2.1 shrinks it).
3. `ENCRYPTION_KEY` versioning — `src/contexts/integration/infrastructure/adapters/token-encryption.adapter.ts` (51 lines): `createTokenEncryptionAdapter({ activeVersion: 'v1', keys: { v1: hexKey } })`; ciphertext `v1:<iv>:<tag>:<ct>` with `setAAD(Buffer.from('v1'))`; `decrypt` parses four parts, selects the key by version, throws `Unknown key version` otherwise (no legacy three-part support — data is disposable). Callers: `src/contexts/integration/build.ts:489`, `src/composition.ts:486`, `google-oauth-exchange-recovery.repository.integration.test.ts:92`. New `token-encryption.adapter.test.ts`: round-trip v1; decrypt a v1 ciphertext with a v2 active key registered; tampered version prefix fails.
4. Do not write a keyring-count ratchet now (16 call sites today); WP2.3 adds it when the count drops to ≤3.

Verification: `pnpm test:unit -- src/shared/architecture src/contexts/integration` green; intentionally add `import OpenAI from 'openai'` to a `src/` file → test fails → revert.

**Executed 2026-09-06.** `pnpm vitest run --project=unit src/shared/architecture src/contexts/integration` → 115 files / 921 passed. Negative control performed: adding `import OpenAI from 'openai'` to `src/shared/config/release-identity.ts` failed `provider-client-singleton.test.ts` naming that exact file, then reverted. Three corrections:

1. **Item 2 was already satisfied and needed the opposite of a trim.** The `googleapis.com` allowlist is exactly the six justified entries with a comment each; `security-headers.ts`, `fixtures.ts` and `in-memory-google-oauth-port.ts` are not in it because the detector already excludes `fonts.googleapis.com` and the testing tree. What was missing is the `fetch(` ratchet, so that was added instead: exactly three Integration modules may call `fetch` (`gbp-provider-fetch.ts`, `google-oauth.adapter.ts`, `google-review-api.adapter.ts`), pinned as a set so a fourth fails rather than quietly joining. The plan's instruction to reduce the file "to its allowlist describe" was not followed: the other describes assert distinct privacy invariants — health routes select no content columns, the activity context reads no content-bearing event fields — that neither `content-free-facts.test.ts` nor `privacy-exfiltration-canary.test.ts` covers, since those are runtime-marker based and these are static.
2. **`createTokenEncryptionAdapter` has four call sites, not three.** The plan named `integration/build.ts`, `composition.ts` and the OAuth recovery integration test; `scripts/perf/load-test.ts:194` also constructs it and only typecheck found it. (`composition.ts` passes `encryptionKey` through config rather than calling the factory, so the real callers are build.ts, the integration test and the perf script.)
3. **The version is bound as AAD, and the test proves what that buys.** Beyond the plan's three cases the suite pins the two ways versioning can be defeated: a ciphertext relabelled to a version the adapter _does_ hold fails GCM tag verification (asserted on `unable to authenticate data`, not a bare throw), and a version label containing the field separator is refused at construction because it would make the four-part split ambiguous.

## Phase 2 — Collapse the topology and its ceremony (weeks 3–7) — roadmap

Entry criterion: Phase 1 complete; `pnpm test:unit` < 3 min — already true at `a9d7ffe1` (86 s), so treat it as a regression guard, not a target to earn. Each WP gets its own execution plan when started; the decisions below are settled now.

**Scope moved between WP2.1 and WP2.2, and the shape of the collapse changed.** Three findings from reading the current tree, all of which the roadmap bullets predate:

1. **The direct-fetch deletion belongs to WP2.2, not WP2.1 — but the order stays 2.1 → 2.2.** `src/composition/google-provider-authority.ts:809,828` only builds `googleAuthorizedProviderExecutor` when `googleApprovalGapDisposition(...) === 'wire'`, and `google-review-api.adapter.ts:131` takes `executor` as optional precisely because of that gate. So the fallback cannot go until the approval gap is gone, which is WP2.2's job. WP2.1's own work — swapping the mTLS transport for in-process factories — needs none of that, so it still runs first. (An earlier revision of this spec reordered to 2.2 → 2.1 on the strength of the first half of that sentence alone; that was wrong, because it would have forced WP2.2 to invent a permit issuer that WP2.1 then immediately deletes.) Order: **2.1 → 2.2 → 2.3 → 2.5 → 2.4**.
2. **The permit row is not ceremony, and WP2.1 still goes first.** `createGoogleExecutionAdmissionService.start` resolves `deps.authority.load(input.permitId)` against `google_execution_permits`, and the only writer of those rows is `googleContentAuthority.admit(...)` — the machinery WP2.2 deletes. That made it look like the row existed only so two processes could agree, and an earlier revision of this spec had WP2.2 delete it. It is wrong: `postgres-permit-authority.ts:176` transitions the permit through `start_google_execution_permit_v3` in the database and `:89` refuses any row not in state `admitted`, which is at-most-once enforcement for a provider call and matters just as much in one process, because the worker retries jobs. So WP2.2 replaces the _issuer_ and keeps the row. WP2.1 still runs first, for the plainer reason that its transport swap needs nothing from WP2.2 while the direct-fetch deletion needs the approval gap gone.
3. **Both sidecar pairs already expose in-process factories, so "into the worker" is a wiring change, not a port.** `services/google-egress-gateway/service.ts:126` exports `createGoogleEgressGateway(deps)` and `services/google-execution-admission/service.ts:207` exports `createGoogleExecutionAdmissionService(deps)`. The sidecars are those two factories plus a bootstrap (`index.ts`, `entry.ts`, `environment.ts`, runtime-secret scrubbing, an mTLS server and a health server). The application reaches them through `createGoogleEgressGatewayHttpClient(createInternalMtlsJsonTransport({...}))` at `google-provider-authority.ts:842-866`. Collapsing = construct the two factories in composition and delete the transport. Every dependency already exists in the app: `db`/pool, `providerEphemeralRedis` (`:289`), `credentialBindingKeys` (`:840`), `clock`, and the `routeTarget` already computed at `:879-885`.

Measured surface (this session, `wc -l` over `git ls-files`):

| cluster                                | lines  | note                                                                                                                              |
| -------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------- |
| Google content-approval ceremony       | 11,054 | 33 files; `google-content-*`, `google-provider-authority`, `capability-refusal`, the executor adapter and its check               |
| `services/google-egress-gateway`       | 1,347  | 8 files                                                                                                                           |
| `services/google-execution-admission`  | 1,936  | 13 files                                                                                                                          |
| `services/ai-egress-gateway`           | 10,235 | 43 files                                                                                                                          |
| `services/ai-execution-admission`      | 1,944  | 15 files                                                                                                                          |
| `services/*.ts` shared sidecar runtime | 3,484  | `internal-mtls` (947+604), `sidecar-*`, `platform-health`, `process-lifecycle`, `google-peer-identities`, `postgres-database-tls` |

**CI genuinely exercises all of it**, which is why the `e2e` job is the gate for every WP below: `scripts/local-stack/stack.ts:814` mints signed approval bundles and runtime bindings for all four `GOOGLE_CONTENT_CAPABILITIES` (`src/shared/auth/google-content-contract.ts:6-11`), so the Compose stack runs the full ceremony — Ed25519 role keys, permits, HMAC grants, mTLS between two sidecars — on every push. Nothing here is inert.

### WP2.1 — Google egress and admission in-process

1. New `src/composition/google-egress-runtime.ts`: build `createGoogleExecutionAdmissionService({ nowMs: clock, admissionId, grantKeyring, grantStore: createRedisGoogleAdmissionGrantStore(redis, nowMs), authority: createPostgresGoogleAdmissionPermitAuthority({ pool, gatewayIdentity, releaseSha }), quotaForPolicy, inFlightForPolicy })` — mirroring `services/google-execution-admission/index.ts:118-177`, including the per-policy `GOOGLE_QUOTA_POLICIES` loop — then `createGoogleEgressGateway({ nowMs, gatewayIdentity, bindCredential, routeTarget, grantKeyring, admission, fetch, logger })` mirroring `services/google-egress-gateway/index.ts:93-110`.
2. Point `google-provider-authority.ts` at that gateway and delete `createGoogleEgressGatewayHttpClient(createInternalMtlsJsonTransport({...}))` (`:842-866`).
3. Env: add `GOOGLE_ADMISSION_GRANT_HMAC_KEYS` and `GOOGLE_EGRESS_GATEWAY_IDENTITY` to `src/shared/config/env.ts`. Drop `GOOGLE_EGRESS_GATEWAY_ORIGIN`, `GOOGLE_EGRESS_GATEWAY_SERVER_NAME`, `GOOGLE_EXECUTION_ADMISSION_ORIGIN`, `GOOGLE_EXECUTION_ADMISSION_SERVER_NAME`, the six `GOOGLE_INTERNAL_MTLS_*`, `GOOGLE_EGRESS_ALLOWED_CALLER_IDENTITIES` and `GOOGLE_ADMISSION_DATABASE_CA_B64`. `routeTarget` needs no new env — composition already derives it.
4. Delete `services/google-egress-gateway/` (1,347), `services/google-execution-admission/` (1,936), `services/internal-mtls.ts` + test (1,551), `services/google-peer-identities.*` (190), `Dockerfile.google-egress-gateway`, `Dockerfile.google-execution-admission`, `tsup.google-egress-gateway{,-local}.config.ts`, `tsup.google-execution-admission.config.ts`.
5. Compose loses six services, not two: `google-execution-admission`, `google-egress-gateway`, `provider-redis`, `provider-redis-ingress`, `google-admission-redis-store`, `google-admission-redis-ingress`, plus `provider-control-proxy` and `Dockerfile.google-provider-redis` if nothing else claims them — grants and quota move onto the Redis the app already opens (`PROVIDER_EPHEMERAL_REDIS_URL`). Confirm each has no other consumer before deleting.
6. `ci.yml`: drop the two Google entries from the sidecar image group.

Verification: local gate; **`e2e` green**; `docker build .` still one image; `git grep -n "internal-mtls\|EXECUTION_ADMISSION_ORIGIN\|EGRESS_GATEWAY_ORIGIN" -- src services scripts .github` → only AI-side hits, which WP2.3 removes.

### WP2.2 — Delete the Google approval-bundle ceremony, its permits, and the direct-fetch fallback

1. Delete the ceremony: `src/shared/auth/google-content-approval.ts` (508), `google-content-authority.ts` (836), `google-content-contract.ts` (223), `google-content-runtime-bindings.ts` (106), `src/shared/domain/google-content-authorization-vector.ts` (165), `src/contexts/identity/infrastructure/repositories/google-content-authority.repository.ts` (739), `src/contexts/integration/infrastructure/google-content-authorization-check.ts` (736), `src/shared/db/schema/google-content-control.schema.ts` (604), `scripts/ops/google-content-approval.ts` (125), and every co-located test. `scripts/ops/google-content-approval-sign.ts` and `scripts/ops/closed-beta-google-content-activate.ts` named by the bullet **do not exist** — already gone.
2. `src/composition/google-provider-authority.ts` (944): delete `googleApprovalGapDisposition`, `buildRefusingGoogleProviderAuthority` (`:207-258`), the four `authorizeGoogle*Content` authorizers and the refusal loggers (`:498-560`), and the `admit` closure at `:886-919`. The executor becomes unconditional.
3. Replace the gate with the two checks the beta actually needs, in one small module: the Google connection for the org/property is `active` (`google_connections.status`), and the compiled route's `catalogueVersion` equals the app's current route-catalogue version. Both are already computed — `admission.catalogueVersion` at `:904` — so this is a comparison, not new machinery.
4. Delete the four-capability union and its policy version (`GOOGLE_CONTENT_CAPABILITIES`, `GOOGLE_CONTENT_CAPABILITY_POLICY_VERSION`) once no reader remains; `src/shared/auth/execution-permit-start-deadline-sweep.ts:27,102` is the last one and loses its `capabilities` default.
5. Env drops: `GOOGLE_CONTENT_APPROVAL_ROLE_PUBLIC_KEYS_JSON`, `GOOGLE_CONTENT_RUNTIME_BINDINGS_JSON`, and the stack's `GOOGLE_CONTENT_LOCAL_APPROVAL_BUNDLES_JSON` generator (`scripts/local-stack/stack.ts:810-943`).
6. Delete the direct-fetch fallback now that the executor is unconditional: `executor` required at `google-review-api.adapter.ts:131`, the three direct fetches at `:689,:962,:1101`, and `assertDirectProviderEgressAllowed` + its comment at `src/shared/config/provider-config-guards.ts:14-19,169-189`. **Keep** `assertDirectCredentialEgressAllowed` (`:192-205`) — `google-oauth.adapter.ts:325,462,516` and `gbp-api.adapter.ts:53` use it.
7. **Permits stay; only the ceremony that decides whether to issue one goes.** An earlier revision of this spec had WP2.2 delete the permit table on the reasoning that a Postgres row only existed to let two processes agree. Reading the authority disproves it: `postgres-permit-authority.ts:176` starts a permit through `start_google_execution_permit_v3`, a state transition in the database, and `:89` refuses any row not in state `admitted`. That is at-most-once semantics for a provider call, and it is load-bearing in one process too — the worker retries jobs, and a retry that re-published a reply because the permit was only an in-memory object would be a product defect, not a tidier design. So the permit row, its authority and the start-deadline sweep all survive; what dies is the approval-bundle machinery that used to decide whether to write one. The replacement issuer writes the same row from the two checks in step 3.
8. ADR 0050 loses §6/§8/§11/§12 and the mutation log; §3/§5/§7/§10 stay verbatim.

**WP2.2 is bigger than the bullet implies, and measured 2026-09-06.** The permit row survives (finding 2) but cannot survive unchanged, and the SQL that reads it enforces that:

- `start_google_execution_permit_v3` is a three-deep chain (`v3` → `v2` → `v1`, `db-constructs.sql:6818,7285,7517`) validating the vector, the policy version and the emergency-kill version against the row, alongside `complete_…_v1` and `fail_…_v1`.
- The issuer is `insertPermit` at `google-content-authority.repository.ts:507-535`, inside the repository being deleted.

Filling the ceremony columns with degenerate constants to avoid touching the SQL would be a shim, and the program rules forbid one.

**It splits, but not in the order that reads naturally.** Two revisions of this note were wrong before the constraint that decides it turned up, so the evidence is recorded with the answer.

The obvious decomposition is "write the replacement beside the ceremony, prove it, then delete" — a replacement can usually run next to what it replaces. Here it cannot: `authorization_execution_permits.approval_binding_id` is `notNull` with a foreign key to `capability_compliance_approvals` and `onDelete: 'restrict'` (`google-content-control.schema.ts:297-299`). A new issuer therefore cannot write a permit at all until that column is gone, and making one up would mean minting a fake approval row — a shim.

A second correction from the same read: **`authorization_vector` is not ceremony and stays.** It is the request binding — `requestBindingSha256`, `credentialBinding`, `projectFingerprint`, `requestBodySha256`, `requestBodyBytes` (`postgres-permit-authority.ts:39-45`) — and it is what stops a permit issued for one request being spent on another. The genuinely ceremony-only columns are `approval_binding_id`, `start_vector_mode`, `commit_vector_mode`, `policy_version`, `emergency_kill_version` and `capability`. That makes the contraction much smaller than "six columns and a chain of three functions" first suggested.

So the order is contraction first, gate second:

1. **Contract the permit**: drop the six ceremony columns, collapse `start_…_v1/v2/v3` into one function, drop the vector-mode and policy-generation parameters, regenerate the baseline (`pnpm db:baseline`; the database is disposable). The ceremony still runs and still authorizes — it simply stops writing columns nobody reads. Behaviour is unchanged, which makes this the safest possible first move, and `e2e` proves it.
2. **Swap the gate**: the new module checks that the connection is `active` and the catalogue version matches, derives the project fingerprint from `GOOGLE_CLIENT_ID` (the Google Cloud project whose quota is being spent — the runtime binding's `googleProjectAttestationSha256` was only ever a proxy for it), and issues the permit. Nothing calls the ceremony afterwards. `e2e` proves the new gate authorizes a real call.
3. **Delete the ceremony.** By now a pure deletion with no caller.
4. **Delete the direct-fetch fallback**, which needs only that the executor is unconditional.

Each step is independently green and revertable, and the one behavioural change is isolated in step 2 between two mechanical ones.

**Step 1 detail, measured 2026-09-06 — the ceremony is re-validated in SQL, and that is where most of it lives.** `start_google_execution_permit_v1` (`db-constructs.sql:6818`, 80 lines) does not merely transition a state. On every permit start it re-derives the whole approval decision inside the database: `policy_version` scope/version/emergency-kill match, `capability_execution_control.denied`, the approval row's `status`/`route_catalog_version`/`execution_policy_version`/`google_project_attestation_sha256`/`approved_at`/`expires_at`, the `railway_closed_beta_cohort` containment, a "no newer binding_version exists" check, organization suspension, `organization_capability` membership, and per-capability principal rules reaching into `member.role` and the authorization vector's `principalKind`/`role`.

Function sizes: `start_…_v1` 80, `_v2` 83, `_v3` 83, `complete_…_v1` 122, `fail_…_v1` 137 — **505 lines of PL/pgSQL** implementing the same ceremony the TypeScript implements above it.

The contracted replacement is one function, roughly 40 lines: the permit is in state `admitted`, its `start_deadline_at` has not passed, its `route_key`/`route_catalog_version`/`quota_policy_id` still match the catalogue, and the `google_connections` row for the org is `active`. Everything else in that list is either a fact the beta does not vary (one organization, one cohort, one policy generation) or a check the application already performs before it asks for a permit.

This is why step 1 is worth doing on its own and doing first: it is the largest single deletion in WP2.2, it is behaviour-preserving for the surviving checks, and `e2e` exercises it on every push. Rewrite the three `start_…` variants into one, contract `complete_…` and `fail_…` to the columns that remain, drop `approval_binding_id`, `policy_version`, `emergency_kill_version`, `start_vector_mode` and `commit_vector_mode` from `authorization_execution_permits`, and regenerate the baseline. Keep `capability` until step 3, when the four-capability union goes — dropping it in step 1 would couple the two steps through `nextPermitGeneration`'s scope key and the start-deadline sweep.

**Step 1 landed 2026-09-06 (`fb23aba8`), and reading the seam shrank the rest of WP2.2 by two thirds.** The deletion list in the bullet counted 11,054 lines of "Google content-approval cluster". Most of that is not the ceremony:

- **`google-content-authorization-check.ts` (736) SURVIVES.** It is the principal resolver, not an approval reader: it stages the authorization vector — `principalKind`, `systemPrincipal`, `role`, `permissionVersion`, `permissionDigest` — for each capability, and it contains **zero** references to `capability_compliance_approvals` or any approval binding. The contracted SQL still reads exactly those vector fields, so deleting it would delete the principal binding the permit is checked against.
- **`google-content-authorization-vector.ts` (165) SURVIVES** for the same reason: vector comparison and the permission digest.
- **The four per-capability authorizers (1,622 lines across review-sync, reply-publication, import and performance) SURVIVE.** They already do the product checks — binding currency, connection usability, a re-read after the token fetch — and consult the ceremony only through `authorizeGoogleContent` for the approval half.
- **The permit, its authority and the start-deadline sweep survive** (finding 2).

What actually dies is the approval half: `google-content-approval.ts` (508, bundles and role signatures), the approval portion of `google-content-authority.ts` (836), `google-content-runtime-bindings.ts` (106), the approval and control tables in `google-content-control.schema.ts`, `scripts/ops/google-content-approval.ts` (125), the ceremony env, and the stack's bundle generator. Roughly 3–4k lines, not 11k.

So step 2 is smaller than written: rewire the four `authorizeGoogle*Content` closures in `google-provider-authority.ts:560-645` to call the surviving principal resolver directly instead of `googleContentAuthority.preauthorize`, drop `approvalBindingId`/`policyVersion`/`emergencyKillVersion` from `GoogleReviewSyncContentAuthorizationResult` and its three siblings, and replace `admit` with a thin issuer that writes the permit from the resolved vector plus `sha256(GOOGLE_CLIENT_ID)` as the project fingerprint.

Verification: local gate; `pnpm test:integration` green; **`e2e` CI job green** (it is the only thing that proves a Google call still reaches the provider); `git grep -n "google-content\|approvalGap\|assertDirectProviderEgressAllowed" -- src services scripts` → none outside `docs/archive`.

### WP2.3 — AI pair into the worker only

Entry criterion: WP2.1 landed, so the Google collapse is the worked example. Same shape: `services/ai-egress-gateway` (10,235) and `services/ai-execution-admission` (1,944) expose in-process factories; the bullet's remaining items (reply drafting as a job, `OPENAI_API_KEY` worker-only via the `production-secrets.ts:57-69` guard, settlement receipts, canary authorization, `sidecarFunctionIsolationSql` in `migrate-deploy.ts`, the keyring cull to ≤3 with a `createVersionedHmacKeyring(` ratchet, the branded-source type test, and the spend-cap boot refusal at `src/shared/ai-openai-provider-profile.ts:116`) stand as written.

### WP2.5 then WP2.4 — build surface, then CI

Both trail the deletions above because they clean up what those orphan. WP2.5: remaining sidecar Dockerfiles and tsup configs (collapse to one `defineConfig([...])`), `check:container-images`, `verify-*-image.mjs`, `smoke-provider-redis-image.sh`, `check:google-runtime-bundle`, `tsconfig` 4 → 2 and `check:typescript-project-coverage`, `deploy-ci-images.ts` to ~400 lines. WP2.4: `ci.yml` to the minimal path plus a nightly workflow, per the bullet. Doing 2.5 first means 2.4 rewrites `ci.yml` once, against a build surface that has already stopped moving.

### Settled decisions the spec above executes (original roadmap bullets)

- **WP2.1 Google trio into the worker** (~2 weeks; ARCH/SEC). Allowlist, permits and quota become modules; grants and OAuth state move to the existing Redis with 15-min TTL (SEC.md row 10: one keyring, keep PKCE/state/session+org binding/one-time/10-min; keep the redeem-tombstone test `oauth-state-handle.test.ts:269`). Delete the direct-fetch fallback: `executor` becomes required at `google-review-api.adapter.ts:131`, the three direct fetches at `:689, :962, :1101` and the guard `assertDirectProviderEgressAllowed` at `src/shared/config/provider-config-guards.ts:169-189` plus its comment `:14-19` go (keep `assertDirectCredentialEgressAllowed` `:192-205`, used by `google-oauth.adapter.ts:325,462,516` and `gbp-api.adapter.ts:53`); WP1.8's allowlist shrinks to the executor module. Delete `services/google-egress-gateway`, `services/google-execution-admission`, `services/internal-mtls.ts` (952), two Dockerfiles, two tsup configs.
- **WP2.2 Google approval-bundle stack** (2–3 days; SEC.md row 1, ADR 0050 §11): delete `src/shared/auth/google-content-approval.ts` (509), `google-content-authority.ts` (836), `src/composition/google-provider-authority.ts` (944), `scripts/ops/google-content-approval-sign.ts`, `scripts/ops/closed-beta-google-content-activate.ts`, `capability-refusal.ts` refusal taxonomy only where it explains bundles; replace with a per-property enabled row + route-catalogue version compare (DATA.md row 6: `google_connections.status` + app-level check); ADR 0050 loses §6/§8/§11/§12 and the mutation log (keep §3/§5/§7/§10 verbatim).
- **WP2.3 AI pair into the worker only** (~2 weeks): reply drafting becomes a job; `OPENAI_API_KEY` read only by the worker (`production-secrets.ts:57-69` guard extended); delete settlement receipts, canary authorization, `services/ai-egress-gateway`, `services/ai-execution-admission`, `sidecarFunctionIsolationSql` in `migrate-deploy.ts`, ~10 of 13 keyrings (then write the ≤3 `createVersionedHmacKeyring(` ratchet); write the branded-source type test and the spend-cap boot refusal (`maximumCostMicros` lives at `src/shared/ai-openai-provider-profile.ts:116`, budget denial at the new in-process service — not `openai-connector.ts:145,176` as the audit says). ADR 0051 folds into the BETA.md checklist after this WP.
- **WP2.4 CI to the lean shape** (1 day; CI.md § minimal path): `ci.yml` → `static` (`check:dependency-audit`, `check:action-pins`, `format:check`, `typecheck`, `lint:ci`), `test-unit` ×4, `test-integration` (+ `db:migrate-deploy` parity + `check:schema-drift`), `artifacts` (web+worker build, `check:security-headers`, `check:language-verifier`, `check:bundles`, `check:production-artifacts`), `docker` (web image only, grype), `secrets`; `check` aggregate name kept. Main push adds the 7 critical e2e journeys (TEST.md row 7 list) with `retries: 1` and no `--fail-on-flaky-tests`, then the image push + digest map. New `nightly.yml` (~60 lines) = full + compatibility e2e, `storybook-test`, CodeQL, Fallow, simulation (`pnpm simulate`), `check:licenses`, `check:test-quality`; delete `codeql.yml`, `fallow.yml`, `simulation.yml`, `simulation-invocation.test.ts`. e2e: delete `error-detection.spec.ts`, the six deployed probes + `run-deployed-critical-journeys` runner (already gone with WP1.5), collapse the P2/P3/kill-switch denials to one test.
- **WP2.5 Sidecar build surface + deploy script** (1 day; CI.md rows 5, 11; DEPS rows 6–7): delete the remaining sidecar Dockerfiles, tsup configs (collapse to one `defineConfig([...])`), `check:container-images` (728), `verify-*-image.mjs`, `smoke-provider-redis-image.sh`, `check:google-runtime-bundle`; `tsconfig` set 4 → 2 and delete `check:typescript-project-coverage`; `deploy-ci-images.ts` 1,020 → ~400 (`--sha`, `--apply` only). If Railway is abandoned (Assumptions), delete `deploy-ci-images.ts`, `railway.json`, `railway.worker.json` instead.

## Phase 3 — Shrink the product code to the surface that exists (weeks 7–12) — roadmap

- **WP3.1 Delete the in-process event bus** (4–5 days; ARCH row 2): `src/shared/events/event-bus.ts`, `src/shared/outbox/{emit-and-record,cutover-flags,shadow-compare}.ts`, every `infrastructure/event-handlers/` directory, `DURABLE_CUTOVER_*` flags; the outbox relay + dispatcher become always-on and the only after-commit path; review→inbox and portal→metric projections are written inside the source transaction; `neverthrow` and its two test importers go here; `no-event-bus-after-db-write.test.ts` already deleted. Needs a backlog watermark before enabling.
- **WP3.2 Delete region/data-cell machinery** (2 days; ARCH row 4, DATA row 5): `src/shared/db/single-us-data-cell-cutover.ts`, `single-us-data-cell-target-binding.ts` (+ `bindSingleUsDataCellCutoverTarget` in `migrate-deploy.ts`), `src/shared/routing/data-cell-execution-fence.ts`, `src/shared/domain/data-cell-catalogue.ts`, `src/contexts/property/**/region-move*`, `scripts/ops/cutover-single-us-data-cell.ts`, tables `region_moves`, `data_cell_topology_cutovers` + 4 routing tables, the 15 `*_topology_cutover_fence` triggers and 4 guard functions in `db-constructs.sql`; ADR 0054 merged into 0057. **Now documentation-backed, not opportunistic:** `docs/BETA.md` §1 states one deployment serving every supported country and carries no cell, region or country-count vocabulary. `DATA_CELL_IDS` declares `us`/`europe`/`global` while `BETA_DEPLOYMENT_DATA_CELL_IDS` is `['us']` and production refuses the other two, so two thirds of the enum is unreachable today. Deliberately dropped with the catalogue: `DATA_CELL_SUPPORTED_COUNTRY_COUNT = 245` and its `libphonenumber-js` country-set SHA, whose only job is stopping a dependency bump from silently changing country→cell placement — a mapping that ceases to exist. Note the naming trap: `global` is a _denied_ cell id, so "one global deployment" must never be written with that word.
- **WP3.3 Schema to ~80 tables** (2–3 weeks, bucket by bucket; DATA rows 3, 4, 7–12): AI ceremony (20 tables, ~50 functions, 29 triggers) → in-process budget check; here the merchant AI notice becomes a TS constant and its text is corrected to the real 24-hour cache retention with a new notice version + re-consent (defect 7); the 103 immutability triggers + 57 guard functions → repository-layer checks; org lifecycle/privacy/backup receipts (12) → one events table; policy tables (5) → config keeping `property_access_grant` + `policy_consent`; metric registry (4) → constants; Better Auth de-duplication (3 tables, six billing columns); write-only tables (4); plumbing receipts (14) → `event_consumer_receipts`. Each bucket: delete schema + constructs → `pnpm db:baseline && pnpm db:reset` → integration suite.
- **WP3.4 Merge contexts** (2 weeks; ARCH rows 5, 6, 8): metric+goal+dashboard → `reporting` (delete the two legacy goal families, 6 tables, `goal.repository.ts`, `governed-goal.repository.ts`, `legacy-goal-inventory*`, `goal-organization-export.adapter.ts`); activity+notification → `feed`; staff → identity `people` (then `'Staff'` leaves the `Role` union and `tenant-resolver.ts:307-310`). Also legacy compatibility surfaces (ARCH row 9): invitation v1 sentinel. **People reconciliation was deleted in WP1.4** — it was team-coupled, so `team`'s deletion forced it forward.
- **WP3.5 Tests to the lean pyramid** (3 days; TEST rows 4, 6, 12): delete the 19 mock-DB store twins and 32 org-lifecycle unit twins; make `src/test-setup.ts` env scrubbing opt-in; target `pnpm test` < 2 min.
- **WP3.6 Frontend consolidation** (1 week; UI rows 4, 5, 7, 8, 11, 12): Storybook to one runner (drop `@storybook/addon-vitest`, `@storybook/addon-mcp`, `@vitest/browser`, `@vitest/browser-playwright`, the `storybook` build job, `vitest.config.ts:22-45`, 12 `ui/*.stories.tsx`; rewrite `.omp/AGENTS.md` Storybook section to the single runner); `<input type="color">` replaces `ui/color-picker.tsx` (1,638); one `Stat` and one `RatingDistribution`; goals matrix on `AvailabilityLine`; inbox flattening (`reply-editor.tsx` pass-through, `use-inbox-detail-queries.ts` merge, `revision_conflict` recovery from the command response instead of polling — UI bug 5); import wizard 35 files → one `importFns` bundle, 2 hooks; beta feedback → one-file text form.
- **WP3.7 Local stack** (1 day; DEPS row 8): `compose.local.yml` 1,077 → ~60 lines (Postgres + Redis, optional MinIO/mail stub), delete `scripts/local-stack/stack.ts` (2,703) and `local:doctor`'s exact-Node refusal; the second Redis (ADR 0053) becomes optional outside production; e2e fixtures rebased on the small compose.

## Phase 4 — Keep it small (ongoing) — roadmap

- Inline single-implementation ports as contexts are touched (51 of 85 `*Port` interfaces; toward −8,000 of 12,927 lines).
- ADR cut per README §6 (13 keep / 11 amend / remaining merges), YAML frontmatter on every retained ADR, `docs/adr/README.md` generated from frontmatter; `src/**/CONTEXT.md` capped at ≈80 lines with the `## Events consumed`, `## Public API`, `## Architecture layers` tables deleted.
- Docs to ≈55 active files; `docs/operations/runbooks.md` split; remaining `*-contraction.md`/`*-cutover.md` archived the day each lands.
- `eslint.config.js` boundaries 1,300 → ~150 lines (keep `cross-context-public-api` and the context-internal import ban); `check:zod-conformance` → a 3-line `no-restricted-imports` entry; Node pinned exactly only in the Dockerfile; drop the byte-attested generated-file carve-outs in `.prettierignore`.
- `package.json` scripts → the 26-item list in DEPS.md § Proposed script list plus `db:baseline`, `db:reset`, `clean`.

## Critical files & anchors

- `scripts/migrate-deploy.ts:107-246` — the deploy runner WP1.3 rewrites around `drizzle-orm`'s `migrate()`; keep the lock, auth track, isolation and completeness check.
- `src/shared/db/staged-drizzle-migrator.ts:9-15,135-152,239-269,379-382` — what the squash makes obsolete (tag cutoff, enum preflights, backfill hook).
- `vite.config.ts:74-95` — rolldown `codeSplitting.groups`; the audit's `:396-400` does not exist.
- `src/routes/_authenticated.tsx:112,195-199` — the only place `'Staff'` is minted client-side; WP1.7 turns it into a refusal.
- `src/shared/governance/entry-point-catalogue.ts` — production data with fail-closed semantics; rows are deleted per WP, its test is deleted in WP1.2.

## Verification (program level)

After Phase 0: `pnpm lint:ci` green with no version pin; `pnpm build && pnpm check:bundles` reports the entry's static closure ≤ 200 KiB; `docker build .` produces the single image.

After Phase 1, from a clean clone with only local Postgres + Redis:

1. `pnpm install && pnpm db:reset` → migration completes from `drizzle/0000_baseline.sql` + `0001_db_constructs.sql` + `0002_db_seed.sql`; `pnpm check:schema-drift` clean. **Satisfied 2026-09-06:** 216 tables after WP1.4, journal 3/3, zero drift, and the control plane seeded (11 metric definitions, 4 AI operation profiles, 1 topology row).
2. `pnpm test` (unit only, no database) green under 3 minutes wall (86 s at the `a9d7ffe1` baseline); `pnpm test:integration` green.
3. `pnpm dev` → `/` redirects to `/login`; log in as the seeded manager (`pnpm seed:e2e-user`) → dashboard renders with the 30-day default; `/inbox` loads; `/home`, `/leaderboard`, `/register`, `/settings/closure` are not found; `/api/health/ready` → 200.
4. `git ls-files | wc -l` and `wc -l` over `src/shared/governance src/shared/release scripts/release .railway drizzle` recorded in the final Phase 1 commit message against the audit's baselines (governance 23,286; release 17,877; scripts/release 18,666; `.railway` 1,952; `drizzle/meta` 2.96 M lines).
5. `git grep -c "" docs/BETA.md` ≤ 120 and every enforced rule in its §5 names a test or lint path that exists. **Satisfied 2026-09-05 by the draft:** 114 lines; every path in §2 and §5 verified to exist; every capability count verified by executing the code. The inverse check is new and stricter — see WP1.2.

**Phase 1 closed 2026-09-06. Every program-level item executed, not asserted:**

1. Clean `dropdb`/`createdb` → `pnpm db:reset` → 216 tables, journal 3/3, `check:schema-drift` clean, control plane seeded (11 metric definitions, 4 AI operation profiles, 1 topology row).
2. `pnpm test` with `DATABASE_URL` unset → 1,096 files / 10,147 passed in **86 s** wall, no database. `pnpm test:integration` → 212 files / 1,156 passed.
3. Browser, against `pnpm dev` with the seeded manager: `/api/health/ready` → 200; `/` → `/login`; login → `/dashboard?timeRange=30d` with the 30-day default; `/inbox` loads its folder rail; `/home`, `/leaderboard`, `/register`, `/settings/closure` all render not-found.
4. Line counts against the audit's baselines:

| measure                 | audit baseline | after Phase 1 | delta      |        |
| ----------------------- | -------------- | ------------- | ---------- | ------ |
| tracked files           | 5,081          | 4,226         | −855       | −16.8% |
| TS/TSX lines            | 806,737        | 698,534       | −108,203   | −13.4% |
| `src/shared/governance` | 23,286         | 10,669        | −12,617    | −54.2% |
| `src/shared/release`    | 17,877         | 808           | −17,069    | −95.5% |
| `scripts/release`       | 18,666         | 0             | −18,666    | −100%  |
| `.railway`              | 1,952          | 0             | −1,952     | −100%  |
| `drizzle/meta`          | 2,960,000      | 120,014       | −2,839,986 | −95.9% |

Migrations 182 → 3 generated files (94 MB → 3.9 MB); database tables 233 → 216. 5. `docs/BETA.md` is 114 lines, and every test or lint path its §5 cites resolves to a file that exists — the eight architecture canaries, `eslint.config.js`, `scripts/check-architecture-boundary-controls.mjs`, `src/contexts/review/domain/rules.test.ts`, the three `atomic-*-outbox` tests and the `vite.config.ts` import-protection block.

**The 13.4% TS reduction is short of the audit's ~30% headline, and the gap is real rather than unfinished Phase 1 work:** the audit counted the whole five-phase program. Phase 3's schema collapse (~250 → ~80 tables) and context merges are where the remaining reduction lives. What Phase 1 actually removed is the _machinery_: the governance tree halved, the release and IaC trees went to zero, and the generated-JSON journal lost 96% of its lines.

### CI green, and the four regressions the local gate could not see

**Full CI green on `23fae9a3` (run 34008838744): all 16 jobs, `e2e` included.** The baseline `a9d7ffe1` was green too, so every break in between was Phase 1's, and the local gate proved insufficient to catch any of the four. Each hid the next behind `--max-failures=1`, so they surfaced one at a time.

1. **The lockfile** (`test-unit`, all four shards, plus every install). WP1.5 removed the `railway` devDependency without regenerating `pnpm-lock.yaml`, so `--frozen-lockfile` failed before a single test ran. The local gate never installs.
2. **Three references to deleted files** (`artifacts`, `docker`, all three image groups, `e2e`). `tsup.config.ts` still bundled `scripts/google-import-final-schema-probe.ts` into the worker, and the `Dockerfile` still ran `scripts/check-google-import-artifacts.mjs` as a build gate. **The local gate never builds a container.** Audited the class rather than the instance: every entry in all 11 tsup configs resolves, and every Dockerfile and `node scripts/…` named in `ci.yml` and `compose.local.yml` exists.
3. **A duplicated ciphertext format** (`e2e`). `e2e/helpers/fixtures.ts` reimplemented AES-256-GCM under a comment reading "must match token-encryption.adapter.ts", and WP1.8's version prefix broke the match. It now calls the adapter; `ciphertext-format-singleton.test.ts` pins one implementation per format, walking test and fixture files because that is where the copy lived.
4. **An import-cycle capture** (`e2e`). The real one, and the reason WP0.3 first turned `e2e` red. `inboxFns` captured its members eagerly, the client build put the Activity/AI fns in a chunk cycling with it, and ESM handed it `undefined` — so the timeline query threw inside `queryFn` and issued no request at all. Route fn bundles are now lazy; `route-server-fn-bundles.test.ts` pins it. `-notification-fns.ts` had the same defect and was only being saved by chunk order.

Two lessons worth carrying into Phase 2, both about the _shape_ of the gap rather than the individual bugs:

- **The local gate does not install, build a container, or run a browser against the real topology.** Three of the four regressions were invisible to `typecheck` + `lint:ci` + `test:unit` + `test:integration` by construction, not by accident. Phase 2's program rules should add `pnpm install --frozen-lockfile` and `docker build .` to the per-WP routine; both are fast and both would have caught #1 and #2 at commit time.
- **A duplicated definition and an eagerly-captured cross-module reference both fail silently and remotely.** Neither produced a build error, a type error, or a server log. The deletions in Phases 2–4 will keep shuffling the module graph, so the two pins added here (`ciphertext-format-singleton`, `route-server-fn-bundles`) are load-bearing for the rest of the program, not incidental to WP1.8.

A third finding is recorded but not fixed, because it is local-only: on this machine the e2e stack's review sync refuses with `runtime_unavailable` from `google-provider-authority.ts:888`, where an absent runtime binding returns without logging. CI does not hit it. That silent return is a real observability gap — the file's own comment already calls out this class — and belongs with WP2.2's Google collapse.

## Assumptions & contingencies

- **Storybook:** default is "keep, one runner" (WP3.6). If you prefer to drop Storybook entirely, WP3.6 instead deletes `.storybook/`, all `*.stories.tsx`, both CI jobs and the ten `@storybook*`/`@vitest/browser*` packages, and `pnpm test-storybook` leaves the local gate.
- **Railway descriptors:** `railway.json`, `railway.worker.json` and `scripts/ops/deploy-ci-images.ts` are kept as the last deployment shape. If Railway is deleted before WP2.5, delete all three there and replace with nothing (the Dockerfile is the deployment contract).
- **Data:** the database is disposable throughout. If that changes before WP1.3 lands, take `pg_dump -Fc` of the production database first and turn WP1.4's schema deletions into a `DROP TABLE` migration appended to the old journal instead of a re-squash.
- **`drizzle-kit generate --custom`:** if the installed drizzle-kit rejects `--custom`, create `drizzle/0001_db_constructs.sql` by hand and append `{ idx: 1, version: '7', when: <ms>, tag: '0001_db_constructs', breakpoints: true }` to `drizzle/meta/_journal.json` in `scripts/db-baseline.ts`.
- **Worker smoke in CI (WP0.3):** if the docker smoke harness cannot override the container command, drop the `worker` matrix entry; the web image is the worker image.
- **`in_memory` retention:** if a later model snapshot accepts `prompt_cache_retention: 'in_memory'`, WP3.3 sets it and keeps the "at most one hour" notice text instead of re-consenting.
- **Branch protection:** if pushes to `main` succeed without WP0.5, skip it.
