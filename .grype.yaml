# BQC-7.7 — grype ignore rules for the container scanning gate (ci.yml
# `docker` job, anchore/scan-action with fail-build: true,
# severity-cutoff: high; policy: docs/operations/security-ci-policy.md).
#
# Zero exceptions is the target. Every earlier entry here was a Debian 12
# bookworm package Debian had marked won't-fix; 2026-09-07 the base moved to
# node:22.23.2-trixie-slim (Debian 13, plan revision R2) and every one of
# them fell away with the distro. An entry may only be added with the finding
# named (package, CVE, grype's own identity line), an owner, a reason and an
# expiresAt, re-justified at the quarterly review and on every base-image bump.
# grype has no native expiry — the dates are enforced by the documented review
# cadence, not by the tool.

ignore: []
