# Integration event handlers

Integration context currently has no event handlers. It produces events (google_account.connected, etc.) but does not consume events from other contexts.

This directory is intentionally empty until cross-context event consumption is needed.

See `src/contexts/integration/CONTEXT.md` for the current event model.
