// Pattern matching — re-exports from ts-pattern
// Use match(...).exhaustive() for all union dispatch.
// Adding a new union member forces a compiler-checked update.
export { match, P, isMatching } from 'ts-pattern'
