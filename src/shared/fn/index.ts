// Shared functional utilities.
// Per conventions: "Wait for the second importer before adding to shared/fn."
// This file is intentionally minimal — add only when genuinely shared across contexts.

/** Type-safe pipe: compose functions left-to-right. */
// Will be populated as contexts are added and common patterns emerge.
