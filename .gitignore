node_modules
.DS_Store
dist
dist-ssr
dist-worker
dist-google-execution-admission
dist-google-egress-gateway
dist-google-import-compatibility
dist-ai-*
dist-provider-services
storybook-static
/test-results/
/playwright-report/
/.last-run.json
*.local
.env
.env.local
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*

# Drizzle migrations ARE committed (migrate-based workflow).
# `pnpm db:generate` -> commit drizzle/ -> `pnpm db:migrate` is the deploy path.

# Test coverage
coverage/

# Pi-lens
.pi-lens/
# --- babysitter managed ---
.a5c/creds.env
.a5c/creds.env.tmp.*
.a5c/logs/
# --- end babysitter managed ---
.a5c/
CLAUDE.md
.pi/reviews/
.pi/
package-lock.json
.beads/
.dolt-data/
.events.jsonl
.events.jsonl.lock
.runtime/
daemon/
deacon/
/events/
logs/
mayor/
reputation_key/
witness/

# fallow analysis cache (regenerated locally; never commit)
.fallow/cache.bin

# test coverage (generated)
/coverage/

# AI tooling output (regenerable; never commit)
graphify-out/
.hermes/
.kilocode/
.impeccable/
.archon/
.claude/
.agents/
skills-lock.json

# Historical AI review output (regenerable; never commit)
docs/reviews/
drizzle.bak/

# Playwright E2E seed state (written by scripts/seed-e2e-user.ts)
e2e/.seed-state.json

# Generated Docker Compose stack environments (mode 0600; removed on teardown)
/.local-stack/

# Operator-held approval role keystore and generated approval bundles
.secrets/
